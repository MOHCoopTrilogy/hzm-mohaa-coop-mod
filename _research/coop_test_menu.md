# Coop 4-player test-ideation menu (94 tests, 10 subsystems)


## player-count scaling + $player array correctness + connection stress

### [P1 / full] Scaling-parity sweep 2P/3P/4P (enemy count + accuracy grows with player count, no array-cast)
- catches: Broken/absent enemy count-scaling (aihandler::coop_tryDuplicateActor duplication), regressed hardcap or maxPlayerScalar clamp, and accuracy-scaling that doesn't track player count. Also surfaces any 'cast array to listener' throw that only fires with 2+ connected.
- drive: New probe coop_scalecheck (gate cvar default 0) in coop_selftest.scr, threaded from main.scr::main like aistate. Every ~12s while cvar==1 it emits its marker reading local.pc=($player!=NULL?$player.size:0), german=level.coop_actorArray["german"].size, replicas=count of that array with flags["coop_isReplica"]==1, maxscalar=level.coop_maxPlayerScalar, hardcap=level.coop_aiScaleHardCap. Harness boots the SAME 2-3 combat maps (e.g. m1l1, m3l1a, t2l1) three times: 2P, 3P, 4P (each with coop_maptest 2 patrol so AI actually engages), leaving coop_aiScale 1 and default coop_aiScaleChance.
- verify: Emit '^~^~^ SCALECHK map=<m> pc=<n> german=<n> replicas=<n> maxscalar=<n> hardcap=<n>'. PASS if, per map, median german/replicas at 4P > 3P > 2P (monotonic within noise) AND german never exceeds hardcap AND accuracyMult (reuse AISTATE targeting as proxy or add acc field) tracks pc capped at maxscalar; also grep-scan the run window: zero 'cast array to listener' and zero 'Script Error' lines. FAIL on flat/inverted counts or any cast error.
- evidence: aihandler.scr:154,170,198,448; officer.scr:1422; maptest_phase2.scr:304  | risk: Low - read-only probe, existing patrol harness. Duplication raises entity load; keep to <=4P and rely on hardcap.

### [P1 / full] Trilogy-wide late-join / reconnect survival sweep (drop + rejoin a client mid-map)
- catches: Crash or array corruption when connected count changes mid-level; manageSetup re-entry failing to re-arm a rejoined client (coop_isActive stuck at -3/NIL); $player reindex faults after a middle index leaves; reconnectPlayer latch (coop_reconnectingPlayerZero) getting stuck.
- drive: Harness boots 4P on each map. Mid-patrol it hard-kills the client-3 process (disconnect), waits ~6s, then relaunches/reconnects client-3. A LATEJOIN probe (cvar-gated) samples pc and how many players have flags["coop_isActive"]==1 on a timer.
- verify: Emit '^~^~^ LATEJOIN map=<m> phase=<before|after_drop|after_rejoin> pc=<n> active=<n>'. PASS if server proc stays alive with no new CrashDump across the drop/rejoin, active drops by exactly 1 on disconnect and returns to the full count after rejoin (coop_isActive back to 1), and no 'cast array to listener'/'Script Error' in the window. FAIL on crash, stuck active count, or cast error.
- evidence: player.scr:185,888,990,1383,1451,1472  | risk: Medium - relaunching a client mid-map can desync; run one map at a time and confirm the reconnect actually landed (active count) before scoring.

### [P1 / full] Progressive join-order stress 1P -> 2P -> 3P -> 4P (each join re-scales, none throws array-cast)
- catches: The classic single-entity-$player-with-2+-connected bug-class: any code path that casts $player as one listener the instant count crosses 1->2 (or 2->3). Confirms scaling re-evaluates on each JOIN, not only at boot.
- drive: Harness starts the host only (1P) on a combat map, then adds one connecting client at a time with ~15s dwell between joins. Reuse the coop_scalecheck probe from test 1 so a SCALECHK lands after each join. coop_aiScale 1.
- verify: Emit SCALECHK after each join (same marker as test 1). PASS if pc increments 1..4 cleanly, german/replicas step UP at the 2P boundary and continue rising, and the log window around EACH join is free of 'cast array to listener' and 'Script Error'. FAIL if any join throws, or scaling stays at the 1P value after players join (re-scale never fired).
- evidence: replace.scr:337,379,421 (SP single-entity end $player); aihandler.scr:154-156  | risk: Low - additive joins only, no teardown.

### [P1 / partial] DBNO / objective ownership when the mid-owner disconnects
- catches: Ghost state when a player who OWNS live state (DBNO downed body, or an assigned objective) leaves while holding it: a downed entity that never clears (blocking LMS/team-revive), an objective stuck owned by a gone player, and array-cast when the departing index is in the middle of $player.
- drive: Extend coop_dbnotest: drive a non-host player into DBNO (coop_dbnoteam style), THEN have the harness disconnect that exact downed client while still down. Probe emits ownership snapshot: number of players flagged DBNO/downed, and whether any objective owner entity is now NULL.
- verify: Emit '^~^~^ OWNERDROP stage=<downed|dropped|settled> pc=<n> downed=<n> dangling=<n>'. PASS if after the drop downed count returns to 0 (state cleaned, not left dangling), dangling objective-owner refs == 0, remaining players keep coop_isActive==1, and no 'cast array to listener'/'Script Error'. FAIL on a stuck downed entity, dangling owner, or cast error.
- evidence: player.scr:990,1383; replace.scr:359-370 (isActive filter in closest scan)  | risk: Medium - orchestrating down-then-disconnect on a specific non-host client is fiddly; verify the target actually entered DBNO (marker) before disconnecting, else the test is meaningless.

### [P2 / partial] Rapid connect/disconnect churn (edict/configstring leak + reconnect-latch race)
- catches: Entity/edict or CS_MODELS configstring leak across many reconnects (relevant given replica head-model dedupe), a stuck coop_reconnectingPlayerZero latch after a churny client-zero departure, and orphaned coop_isActive/DBNO state left by a player who never cleanly spawned.
- drive: Harness connects and disconnects client-4 for ~10 fast cycles (connect, ~3s, disconnect, ~2s) on one map, sampling server RSS via the process-memory sampler each cycle. A CHURN probe emits count of $player and actorArray size each cycle.
- verify: Emit '^~^~^ CHURN cycle=<i> pc=<n> german=<n> mem=<kb>'. PASS if server survives all cycles (no CrashDump, proc alive), RSS returns to a stable plateau (no monotone climb across cycles beyond a small band), pc settles correctly after the final state, and no 'Script Error'. FAIL on crash, unbounded memory growth, or a latch that blocks the final reconnect.
- evidence: player.scr:1472,1475 (coop_reconnectingPlayerZero latch); aihandler.scr:188-227 (configstring dedupe)  | risk: Medium - timing-sensitive; a too-fast reconnect can hit the reconnectPlayer stufftext window. Space cycles and treat a single failed reconnect as retry, not fail.

### [P2 / full] player_origin crash-trap guard (no coop path invokes the deliberate hang)
- catches: Any regression that routes a live coop caller into replace.scr::player_origin, which is an intentional infinite while(1) crash trap - a silent server hang/CPU spin rather than a clean error.
- drive: No new probe needed. Run the standard 4P trilogy patrol sweep (coop_maptest 2) and additionally the DBNO/win probes, while the log watcher greps for the trap's own println.
- verify: Watch for the literal line 'player_origin - outdated func used, crashed game on purpose'. PASS if it NEVER appears across the whole 4P sweep. FAIL (and flag which map/thread preceded it) on any occurrence; also treat a patrol that stops emitting AISTATE while the proc pegs CPU as a corroborating hang signal.
- evidence: replace.scr:465-470  | risk: Low - passive log-watch layered on existing sweeps; if it ever fires the server is already hung, so kill/restart on detection.

### [P2 / partial] All-players-unavailable -> player_closestTo NULL path (escort/AI NULL-deref)
- catches: NULL-deref Script Errors in callers of player_closestTo (main.scr:726/749/902 etc., escort AI) when the function legitimately returns NULL because every player is dead/spectator/noclip - the classic 'no valid player' edge the 1P case hides.
- drive: On a map with escort/closest-driven AI, use the server-side origin teleport + a probe that either kills all 4 players simultaneously or sets flags["coopDevNoclip"] on all 4, so every player fails the closest-scan filter, then let the AI threads tick for ~15s and restore.
- verify: Emit '^~^~^ NULLCLOSEST map=<m> phase=<blackout|restored> errs=<n>' where errs is Script-Error lines counted in the blackout window. PASS if errs==0 and the server recovers cleanly when players are restored (AI re-acquires). FAIL on any NULL-deref Script Error while all players are unavailable.
- evidence: replace.scr:359-371; aihandler.scr:389,398 (early-out guards to compare against)  | risk: Low-medium - killing all players can trip LMS/end-of-round; pick a map with lives disabled (coop_lmsLives 0) and restore promptly.

### [P3 / full] Hardcap + maxPlayerScalar ceiling at 4P (entity-budget clamp holds)
- catches: Duplication overflowing the entity budget: the aihandler:198 hardcap break failing, or maxPlayerScalar not clamping extra-copy count, which under a full 4P lobby is a known slot-stomp/crash vector.
- drive: Boot 4P on a dense-spawn combat map twice: once with a deliberately LOW coop_aiScaleHardCap and default maxPlayerScalar, once with hardcap high. coop_aiScaleChance 100 to force maximum duplication. Reuse SCALECHK probe.
- verify: Use SCALECHK german/hardcap fields. PASS if german.size NEVER exceeds coop_aiScaleHardCap in the low-cap run (clamp holds) and replicas per original never exceed maxscalar-1, with no CrashDump. FAIL if german blows past the cap or the server crashes under max-duplication.
- evidence: aihandler.scr:169-205 (maxExtra/hardcap break); memory: entity_pool_2048  | risk: Medium - chance 100 + dense spawns is the heaviest load case; that is the point, but watch for the entity-pool 2048 ceiling and treat a clean hardcap clamp as the success signal.


## Engine-level stress + resource limits + crash guards (entity pool 2048 / GENTITYNUM_BITS 11, MAX_SOUNDS 1280, MAX_MODELS 2048 configstrings, MAX_GAMESTATE_CHARS, g_droppeditemlife, TGA/NULL crash guards, map-transition archive)

### [P1 / full] Entity-pool exhaustion drive toward 2048 (4P mass-spawn + drop churn)
- catches: Regression of the world-stomp crash family (bugs 914-927): AllocEdict handing out ENTITYNUM_WORLD (slot 2046) under pressure and stomping live occupants, OR the honest-exhaustion path degrading into a hard crash instead of the intended clean ERR_DROP. Also validates that 4 players' dropped-weapon churn (weapons-on-back = 2-3 drops per dead AI) plus enemy count-scaling duplication never silently corrupts near-ceiling slots.
- drive: New probe coop_entstress in coop_selftest.scr, gated on cvar coop_entstress (default 0). seta coop_entstress 1 + seta coop_aiScaleHardCap high (e.g. 200) + set enemy count-scaling to max in server boot cfg; pick the heaviest AI map (m3l2 5-HOLD battalions or m1l1 officer waves). Probe loops: repeatedly `spawn` throwaway trigger/model entities in batches (respecting a self-imposed ceiling so the test is bounded) while 4 connected players kill count-scaled AI so their drops (item.cpp:516 / weapon.cpp:2623 EV_Remove g_droppeditemlife) cycle. Each batch the probe reads globals.num_entities-equivalent (walk $player-independent entity count via a server count) and emits a marker. Also set g_droppeditemlife low (e.g. 2) in one run and high (60) in another to compare fill pressure. Harness watches for the engine's own EDICTHI line (level.cpp:1754-1758).
- verify: PASS: probe emits `^~^~^ ENTSTRESS batch=<n> spawned=<total> peakslot=<s>` climbing, and any engine `^~^~^ EDICTHI alloc slot=<slot> num_entities=<n> max=<m>` (level.cpp:1756) shows slot strictly < 2046 (ENTITYNUM_WORLD) - never repeated handouts of the same high slot. FAIL: any `INVSTALE pruned` (sentient.cpp:1122) or `INVDUP add refused` (sentient.cpp:986) line during the run (proves a slot got stomped/aliased), OR a CrashDump / non-clean proc exit, OR EDICTHI slot == 2046 recurring. On genuine exhaustion the ONLY acceptable failure is a clean `Level::AllocEdict: no free edicts` ERR_DROP (level.cpp:1745) with the server dropping the map, not crashing the process.
- evidence: openmohaa-hzm/code/fgame/level.cpp:1733,1744,1745,1754-1758; qcommon/q_shared.h:1667-1674; fgame/sentient.cpp:986,1122; fgame/item.cpp:516; fgame/weapon.cpp:2623; coop_mod/aihandler.scr:198  | risk: Intentionally pushes the entity pool to its ceiling on the heaviest map with 4 players; a real regression will ERR_DROP or crash the server mid-run (that IS the signal). Bound the probe's own spawn count so a passing run recovers. No persistent state touched.

### [P1 / full] Sound-index saturation toward MAX_SOUNDS 1280 (many simultaneous 4P SFX)
- catches: CS_SOUNDS configstring pool overflow silently dropping sounds (SV_FindIndex returns 0 = 'no sound' past the cap, sv_init.c:194-197) - the m1l1-truck / m3l2 HD-audio class of bug where combat VO + ambience + vehicle + 4-player weapon fire push unique registered sounds past 1280 mid-mission and audio just vanishes. Also confirms the 11-bit sound_index protocol path stays consistent across exe+cgame+game.
- drive: New probe coop_sndstress (cvar-gated, default 0) in coop_selftest.scr on the most sound-dense map (m3l2 with every HD audio pack, or m1l1 officer fight). Probe force-triggers a large rotating set of distinct sound aliases server-side (loopsound / playsound of many unique wavs incl HD gunsounds + VO pools) while 4 players fire weapons and count-scaled AI generate combat VO - maximizing UNIQUE sound registrations, not just replays. Probe periodically emits the current registered-sound high-water. Harness greps the server log for the engine overflow warning.
- verify: PASS: probe emits `^~^~^ SNDSTRESS registered=<n> peak=<p>` with peak approaching but the run producing ZERO `WARNING: SV_FindIndex overflow (max=1280, start=<CS_SOUNDS>)` lines (sv_init.c:195). FAIL: any SV_FindIndex overflow WARNING for CS_SOUNDS during normal-content play (means the 1280 cap is too low for real 4P load and sounds are being dropped), or a crash. Distinguish deliberate over-saturation (probe intentionally exceeds - expected warning, logged as INFO) from organic-content overflow (real bug).
- evidence: openmohaa-hzm/code/qcommon/q_shared.h:1695 (MAX_SOUNDS 1280); server/sv_init.c:194-197,228-235; qcommon/q_shared.h:1687 (sound_index 11 bits)  | risk: Low - saturating the sound pool degrades to silent-drop (return 0) by design, not a crash. Main risk is log noise; gate the intentional-overflow phase behind a separate marker so it isn't mistaken for a real regression.

### [P1 / full] Multi-hour 4-player soak on heaviest maps (memory growth + crash-dump watch)
- catches: Slow leaks and latent stale-slot corruption that only manifest over hours: unfreed configstring bytes, entity-slot fragmentation, the stale-inventory family (bugs 915/919/920/925) accumulating under sustained combat, and any monotonic server-memory growth. This is the acceptance gate that the 2048/1280 protocol ops + ~25 NULL guards actually hold under real duration, not just a burst.
- drive: Harness boots a 4-player listen server on the heaviest map (m3l2 or m1l1 officer fight) with coop_maptest 2 (existing teleport patrol) driving continuous AI engagement, count-scaling on, g_droppeditemlife 60. Let it run 2-4+ hours. Harness samples server process working-set memory at a fixed interval and records it, watches CrashDumps folder + proc exit, and greps the log for the guard markers. Optionally a lightweight coop_soakbeat probe emits a heartbeat with entity/model/sound counts each minute for trend data.
- verify: PASS: no CrashDump, proc stays alive to the time budget, memory working-set plateaus (growth rate flattens, not linear-to-OOM), and ZERO recurring `INVSTALE pruned` / `INVDUP add refused` (a few isolated prunes are the guard doing its job; a rising rate is a producer regression). Emit harness-side `^~^~^ SOAKBEAT t=<min> rssMB=<m> ents=<e> models=<md> snds=<s>` per sample. FAIL: crash dump, monotonically climbing rssMB with no plateau, or climbing INVSTALE/EDICTHI rate.
- evidence: openmohaa-hzm/code/fgame/sentient.cpp:986,1122,708,762; fgame/level.cpp:1754-1758; memory note entity_pool_2048 (g_droppeditemlife 60, PruneStaleInventory)  | risk: Long wall-clock cost (hours) and it deliberately runs the heaviest content continuously; low correctness risk since it only observes. Schedule off-peak. A real leak ends in an OOM/crash which is the intended signal.

### [P2 / full] Configstring / model-pool pressure on heavy maps (CS_MODELS 2048 + gamestate chars)
- catches: CS_MODELS overflow (the e1l2 class, bug-866/bug-44: props/enemies registering as model 0 = invisible) and MAX_GAMESTATE_CHARS (98304) exhaustion - string bytes for every new model/sound/head-skin combo accumulate and are NEVER freed during a map. 4-player count-scaling duplicates AI and each clone with a unique head/skin combo (aihandler.scr:190-192) registers a fresh CS_MODELS slot, so a full lobby on a heavy map is the worst case for both the model pool and the gamestate char pool.
- drive: New probe coop_csstress (cvar-gated) on the most model-dense map (e1l2, or m3l2 officer waves). Boot with 4 players, coop_aiScaleHardCap raised, count-scaling max, and force many officer/reinforcement waves so unique model+head+skin combos register. Probe periodically walks and emits the count of distinct registered models and (proxy for) gamestate char usage. Run a long-officer-fight variant to accumulate configstrings without a map reset.
- verify: PASS: `^~^~^ CSSTRESS models=<n> peakmodel=<p>` stays < 2048 and no invisible-entity symptom; no `SV_FindIndex overflow (max=2048, start=<CS_MODELS>)` (sv_init.c:195 via SV_ModelIndex) and no gamestate-char Com_Error. FAIL: CS_MODELS overflow WARNING (invisible props regression), any `MAX_GAMESTATE_CHARS` / `SV_SetConfigstring: bad index` (sv_init.c:82) error, or crash. Cross-check invisibility by having the probe spawn a known model and confirm its returned index != 0.
- evidence: openmohaa-hzm/code/qcommon/q_shared.h:1680 (MAX_MODELS 2048),1714 (MAX_CONFIGSTRINGS 4096),1732 (MAX_GAMESTATE_CHARS 98304); server/sv_init.c:82,194-197,209-211; coop_mod/aihandler.scr:190-192  | risk: Low-moderate. Accumulating configstrings on a no-reset long fight is the point; a true regression surfaces as a clean ERR_DROP or invisible entities, not a hard crash. Keep the run bounded by wall-clock.

### [P2 / partial] Rapid map-transition churn (persistent-archive stress)
- catches: Corruption/leak/crash across many back-to-back map loads: the persistent-archive path on real mission end (global/missioncomplete.scr -> coop_mod/missioncomplete.scr -> bsptransition) versus the clean stuffsrv 'map X' (SV_Map) path, plus configstring/entity/sound pools not being fully reset between maps. Catches cumulative growth that a single-map soak misses and validates transition mechanics documented as crash-prone (bsptransition/persistant archive).
- drive: Two harness variants. (A) SV_Map churn: loop stuffsrv 'map <next>' through a rotation of the heaviest maps (as maptest already does) with 4 players connected, cycling fast (short dwell) for many iterations. (B) Real-win churn: use existing coop_wintest probe to fire the REAL missioncomplete/bsptransition path per map, chaining coop_wintest_next through a short rotation, so the persistent archive actually runs each hop. Harness samples memory at each transition boundary and watches for crash dumps + Script Error lines.
- verify: PASS: every transition emits its existing marker (`^~^~^ MAPTEST2_ADVANCE <cur> -> <next>` or the wintest transition marker) and the next map's `^~^~^ MAPTEST_LOADED <map>`/coop-init line appears; memory at each same-map boundary across cycles is stable (no per-cycle ratchet); no CrashDump; no `Script Error` during archive/restore. FAIL: a transition that never produces the next MAPTEST_LOADED (hang), a crash dump on the bsptransition path specifically (variant B), a per-cycle memory ratchet, or Script Error on persistent-archive restore.
- evidence: CLAUDE.md map-transition rules (stuffsrv 'map X' = SV_Map vs bsptransition); coop_mod/maptest_phase2.scr:272,277 (ADVANCE/PHASE_END markers); coop_selftest.scr coop_wintest (real missioncomplete path)  | risk: Variant B exercises bsptransition/persistant archive which project notes flag as crash-prone on a live coop server - that's the point, but expect it to be the most likely to actually crash; run it isolated. Variant A (SV_Map) is safe and fully automatable; Variant B is partial because a crash mid-archive may leave the server needing a harness restart.

### [P3 / partial] LoadTGA / bad-asset crash guard (listen-host renderer resilience)
- catches: Regression of the LoadTGA ERR_DROP crash guard (unsupported image_type / colormap / pixel_size now warn+default-texture instead of ERR_DROP) - a malformed or unsupported TGA in a coop/HD asset pack must not drop the listen host. 4P-relevant because the host renders while 3 clients depend on the session staying up; a host renderer ERR_DROP kills everyone's game.
- drive: Place a deliberately-malformed TGA (unsupported image_type, e.g. 0/9, or bad pixel_size) at a private coop texture path referenced by a test tik, in a maintt-only test pk3. Boot the 4-player listen server on a map that loads that asset (or a probe that precaches/attaches it). Since sustained client button-holds aren't automatable, this only needs the host to LOAD the asset, which the probe/map can force server-side.
- verify: PASS: harness greps `LoadTGA: unsupported image_type` / `colormaps not supported` / `unsupported pixel_size ... - skipping (using default texture)` (tr_image_tga.c:109/116/123, tr_image.c:1677/1684) AND the server/host process stays alive with the map fully loaded (a subsequent MAPTEST_LOADED or heartbeat marker fires). FAIL: process exit / CrashDump, or a `LoadTGA: ... ERR_DROP` drop that tears down the session.
- evidence: openmohaa-hzm/code/renderercommon/tr_image_tga.c:104,109,116,123; renderergl1/tr_image.c:1628,1677,1684  | risk: Low. Requires authoring one malformed asset in a throwaway test pk3 (don't ship it). The dedicated headless path may not run the renderer at all - memory note flags dedicated smoke as broken - so this must run on the LISTEN host (which does render), hence partial/needs the host client up.


## Vehicles, rides, mounted turrets (jeep/halftrack/truck seating, player-driven tank, 2nd-player tank MG gunner) - 4-player dimension

### [P1 / full] t2l2 multi-rider seat census (4 distinct seats, none unseated)
- catches: Seat-assignment array bugs with 4 players: two players colliding on one coop_seatIdx, a player left NIL (unseated -> vehicle drives with an empty/half-empty bed), seat-1 cannon not claimed (halftrack drives with no gunner), or the '$player cast array to listener' throw when single-index code runs with 2+ connected. This is the core 4-player seating path that the existing maptest_vehicle.scr harness never exercises (it only ever touches $player[1]).
- drive: Boot listen server on t2l2 with all 4 players connected BEFORE coop_setup seats them (that is the real order: main.scr::main -> waitForPlayer -> coop_setup at t2l2.scr:1113, dummies spawned coop_setupAttach t2l2.scr:1230, seat loop coop_playerSpawned t2l2.scr:1370+). No button input needed - seating is automatic on playerspawn (t2l2.scr:1140-1143). Add a new probe label coop_vtest_4p_census (gated on a new dev cvar coop_vtest_4p==1, inert for players) in coop_mod/maptest_vehicle.scr: wait ~8s after 4th spawn, then loop i=1..$player.size and read $player[i].flags["coop_seatIdx"], .flags["coop_inJeep"], and whether it is solid; also record level.coop_seatTaken[] to detect duplicate ownership. Sample each player.origin twice 3s apart vs level.coop_seatVeh[seat].origin delta to confirm the player is actually gliding WITH the vehicle (seated) not left behind.
- verify: Emit one line per player: '^~^~^ VT4_SEAT p=<n> idx=<seatIdx|NIL> cannon=<0|1> follow=<0|1>' and a summary '^~^~^ VT4_CENSUS map=t2l2 players=<N> seated=<N> distinct=<N> cannon=<0|1>'. PASS = seated==players, distinct==players (no two share an idx), cannon==1 (seat 1 turret slot 0 taken), every follow==1. Any idx=NIL, distinct<players, or follow=0 is a FAIL. Also grep 'Script Error' / 'cast .* listener' in qconsole.log = hard fail.
- evidence: maps/t2l2.scr:1123 (coop_seatCount=16), :1408-1416 (lowest-free-seat assignment), :1417-1418 (NIL overflow = unseated continue), :1420-1432 (seat 1 cannon attachturretslot 0), :1488-1494 (coop_seatTaken sweep)  | risk: low - read-only snapshot, no state mutation; invincibility already set by the vehicle harness.

### [P1 / full] Rider survives the scripted launch lurch (seat-shield telefrag guard)
- catches: The exact regression coop_seatShield exists to prevent: a glued SOLID passenger gets telefragged/crushed when the truck or halftrack SNAPS to its path or hard-accelerates (both are scripted rails). With 4 solid riders on the bed, a broken/mis-ranged shield kills riders on every launch. Also catches the shield failing to RESTORE solidity afterward (riders stay notsolid = invulnerable cargo forever, i.e. exploit).
- drive: 4 players seated on t2l2 (seats 2-16 are bed, per t2l2.scr:1233). From a probe, record each rider's health, then invoke the same launch path that wraps coop_seatShield 7 16 / 2 6 (t2l2.scr:796,937,958) - either wait for the natural scripted launch or force the vehicle move the map already triggers. Sample rider health across the lurch window (~4s, the shield's re-solid delay is 'wait 3' at t2l2.scr:778) and read each rider's solid state at t+1s (should be notsolid, shielded) and t+5s (should be solid again, coop_riderSolid default 1).
- verify: '^~^~^ VT4_LURCH p=<n> hpDrop=<delta> shieldedMid=<0|1> resolidEnd=<0|1>'. PASS = every hpDrop==0 (no crush death during launch), shieldedMid==1, resolidEnd==1. Any rider hpDrop>0 or a 'player died'/respawn marker during the window = FAIL (telefrag regression). resolidEnd==0 for all = shield-restore broken.
- evidence: maps/t2l2.scr:760-793 (coop_seatShield/coop_seatUnshield: solid->notsolid across lurch, wait 3 re-solid), :796/:937/:958 (launch call sites), :1444-1452 (coop_riderSolid gate)  | risk: low-medium - you deliberately drive a real vehicle launch; keep players invincible-from-combat but you must NOT mask the crush (the test needs real telefrag damage to register, so scope invincibility to enemy fire only or diff health precisely across the exact lurch frames).

### [P1 / partial] 2nd player mans the King Tiger MG while tank auto-drives (m5l2b)
- catches: The whole tank-MG-gunner feature being silently inert: coop_selectGunnerForTank ends immediately if the tank model has no turret slot 1 (vehicles_thinkers.scr:180), so if the King Tiger tik lacks the MG slot NO second player can ever gun and it fails with zero errors. Also catches the gunner claim never taking (attachturretslot 1) and the gunner not being kept distinct from the driver/tank.
- drive: m5l2b: the tank auto-drives and level.coop_playerTank is set to $playertank (the ENTITY, not a player - m5l2b.scr:118), so the driver-select loop exits immediately and the gunner is the only human boarder. Probe: first assert $playertank QueryTurretSlotEntity 1 != NULL; then force-board player 2 by calling `$playertank thread global/vehicles_thinkers.scr::coop_setGunnerForTank $player[2]` (bypasses the useheld/proximity SELECT loop that a minimized client cannot satisfy). Poll level.coop_tankGunner and sample $player[2].origin vs $playertank.origin over 8s to confirm it rides along.
- verify: '^~^~^ VT4_TANKMG map=m5l2b slot1=<0|1> gunner=<pnum|none> distinctFromDriver=<0|1> follow=<0|1>'. PASS = slot1==1, gunner==2, distinctFromDriver==1 (gunner != level.coop_playerTank), follow==1. slot1==0 = MG slot absent on model (feature dead). gunner==none after force = attach failed.
- evidence: global/vehicles_thinkers.scr:177-204 (coop_selectGunnerForTank, slot-1 gate at :180), :208-248 (coop_setGunnerForTank attachturretslot 1), :255 (wired in player_tank_health); maps/m5l2b.scr:100 (QueryTurretSlotEntity 1), :118 (coop_playerTank=$playertank)  | risk: low - force-board mirrors the harness's existing coop_setDriverForTank approach; gunner is intentionally damageable so keep it invincible during the ride poll. The real useheld+proximity SELECT loop (vehicles_thinkers.scr:186-199) is NOT reachable from a minimized client -> flag the human spot-check below.

### [P1 / full] Seat persists across a rider's death->respawn (mid-array rider)
- catches: Seat loss / seat theft on respawn: t2l2 deliberately KEEPS coop_seatIdx across death (t2l2.scr:1327) so a respawner lands back in THEIR seat. Catches a respawning rider (a) losing the seat and going unseated, (b) being reassigned a DIFFERENT seat, or (c) another player's seat getting stolen because the dead player's slot was freed prematurely. Uses a MIDDLE-of-array rider (player 3) to expose index-shuffle bugs a first/last-only test misses.
- drive: 4 players seated on t2l2. Probe records player 3's coop_seatIdx, then kills player 3 (self.health=0 or the harness kill). coop_playerJustDied runs (keeps seat, clears coop_inJeep if cannon - t2l2.scr:1326-1334); on respawn coop_playerSpawned re-seats (t2l2.scr:1140-1143,1407-1418). After respawn, re-read player 3's coop_seatIdx and confirm origin follows that seat's vehicle. Also re-census the other 3 to confirm none shifted.
- verify: '^~^~^ VT4_RESEAT p=3 before=<idx> after=<idx> kept=<0|1> othersStable=<0|1> follow=<0|1>'. PASS = after==before (kept==1), othersStable==1 (players 1,2,4 unchanged), follow==1. after!=before = seat drift; othersStable==0 = a respawn stole/shuffled another rider's seat.
- evidence: maps/t2l2.scr:1326-1334 (coop_playerJustDied KEEPS seat, only clears cannon inJeep flag), :1407-1418 (respawn keeps existing idx, only assigns if NIL), :1420-1452 (re-attach to kept seat)  | risk: low - a single scripted kill + natural respawn; no engine-level risk.

### [P2 / partial] Driver + gunner concurrent occupancy, no slot contention (t3l2)
- catches: The driver-vs-gunner race: both claims key off useheld+proximity<300 of the SAME $coop_tankOffset (vehicles_thinkers.scr:68 and :190-191). Catches two players grabbing the driver slot, the gunner select racing the driver's USE press, or level.coop_playerTank / level.coop_tankGunner ending up as the same player / both trying turret work. Also validates the driver-only state (hide/notsolid/nodamage) vs the gunner's exposed state coexisting.
- drive: t3l2 (player-driven T-34 - driver-select loop is live here, unlike m5l2b). Probe: force player 1 into driver via `$playertank thread ...::coop_setDriverForTank $player[1]`, wait for level.coop_playerTank==$player[1], then force player 2 into gunner via `...::coop_setGunnerForTank $player[2]`. Guard first that $coop_tankOffset != NULL (else driver silently no-ops - the documented t3l2 risk). Sample both players following the tank for 10s.
- verify: '^~^~^ VT4_DUALCREW map=t3l2 driver=<pnum> gunner=<pnum> same=<0|1> driverHidden=<0|1> gunnerExposed=<0|1> bothFollow=<0|1>'. PASS = driver==1, gunner==2, same==0, driverHidden==1 (nodamage+hide+notsolid), gunnerExposed==1 (visible+damageable), bothFollow==1. same==1 = contention bug; driverHidden==0 or gunnerExposed==0 = crew-state bleed.
- evidence: global/vehicles_thinkers.scr:57-83 (driver select loop), :88-158 (coop_setDriverForTank: hide/nodamage/notsolid at :102-104), :184 (gunner offered only when coop_playerTank!=NULL), :190 (gunner must != coop_playerTank), :218 (gunner deliberately exposed)  | risk: low - force-board both slots; the real simultaneous USE contention (two clients holding USE in the same frame near the tank) can only be reproduced by humans -> spot-check flag.

### [P2 / full] Disconnected rider's seat frees and a late-joiner claims it
- catches: Orphaned/leaked seats: when a rider disconnects, its SafePtr goes NULL and the seat must free (coop_seatTaken[idx]->NIL) so the count stays right and a late-joiner can be seated. Catches (a) a dead/leaked seat that no new player can ever claim (late joiner rides unseated/on foot behind a moving convoy), (b) the disconnect handler freeing the WRONG seat, (c) coop_seatCount drift after churn.
- drive: 4 players seated on t2l2. Kill the player-4 client PROCESS (harness disconnect, not in-game death). The 'gone' handler frees the leaver's seat (t2l2.scr:1339-1343) and the periodic SafePtr==NULL sweep in coop_playerSpawned reclaims it (t2l2.scr:1488-1494). Then reconnect a client (late join). Probe snapshots level.coop_seatTaken[] before disconnect, after disconnect, and after the late-join seats.
- verify: '^~^~^ VT4_CHURN freedIdx=<idx|none> reclaimed=<0|1> lateSeated=<0|1> lateIdx=<idx> distinct=<0|1>'. PASS = freedIdx==player4's old idx, reclaimed==1, lateSeated==1, distinct==1 (late joiner did not double-book a live rider's seat). freedIdx==none = seat leak; lateSeated==0 = late joiner stranded.
- evidence: maps/t2l2.scr:1339-1343 (free leaver's seat on 'gone'), :1488-1494 (SafePtr NULL sweep reclaims seat), :1408-1416 (late joiner takes lowest free seat)  | risk: medium - kills and re-adds a client process; the harness already supports process kill/restart and late-join, but verify the reconnect actually re-enters the same live map (not a fresh boot).

### [P2 / partial] Tank driver EXIT restores a playable state (no stuck-spectator)
- catches: A player who leaves the tank being left broken: coop_setDriverForTank's exit path sets the player to spectator, ungluensures, detaches, teleports to $coop_tankExit, takedamage, and rejoins via coop_reJoinTankDriver (vehicles_thinkers.scr:122-148,162-169). Catches the exiting driver getting stuck as a spectator, keeping notsolid/nodamage/hidden, or spawning with an invisible/missing weapon (the documented 'weapon invisible on vehicle exit' bug the forcetorsostate PUTAWAY_MAIN fix targets).
- drive: t3l2: force player 1 into the driver slot, ride briefly, then trigger the exit branch (set level.coop_tankLastDriverTime into the past so the leave cooldown passes, then drive the exit - e.g. from the probe break the ride loop the same way a USE-leave would, or call the exit tail directly). After exit, probe reads player 1: is it back on team allies, solid, takedamage restored, visible (not hidden), and holding a real weapon (not spectator, not empty-hands).
- verify: '^~^~^ VT4_TANKEXIT p=1 spectator=<0|1> solid=<0|1> visible=<0|1> hasWeapon=<0|1> atExit=<0|1>'. PASS = spectator==0, solid==1, visible==1, hasWeapon==1, atExit==1 (near $coop_tankExit). Any spectator==1 / hasWeapon==0 = the stuck-exit regression.
- evidence: global/vehicles_thinkers.scr:122-148 (exit: spectator, unglue, DetachDriverSlot, tele, takedamage, coop_reJoinTankDriver), :162-169 (coop_reJoinTankDriver: skipTeamAndWeaponSelect + forcetorsostate PUTAWAY_MAIN weapon-visible fix)  | risk: low-medium - exercises the spectator->rejoin transition; the true trigger is a client USE-hold-to-leave which a minimized client cannot send, so the exit is forced from script -> flag a human spot-check that a real USE-leave lands the player identically.

### [P3 / partial] Vehicle destroyed / vanish does not strand riders (clean restart)
- catches: Players left glued to an orphaned/dead vehicle when the ride vehicle is destroyed or removed: t2l2 has explicit jeep_gone_watch / destroyed watchers (t2l2.scr:1038-1039) that must restart cleanly rather than stranding riders on a NULL-origin seat, and the player-tank death path reloads the map (vehicles_thinkers.scr:298-320). Catches riders frozen in place / falling through world / stuck notsolid after the vehicle disappears.
- drive: Two variants. (a) t2l2 seat 4 riders, then force level.playerjeep (halftrack) to death/removal and watch the gone/destroyed watcher. (b) m5l2b/t3l2 with a driver+gunner aboard, force $playertank waittill death (health=0) and confirm player_tank_health fires the reload path (vehicles_thinkers.scr:298 'self waittill death' -> replace.scr::missionfailed at :319). Probe watches for the restart marker and that no player is left glued (unglue/detach ran) before restart.
- verify: '^~^~^ VT4_VEHLOSS map=<m> case=<jeep|tank> restart=<0|1> stranded=<n>' where stranded = count of players still glued/notsolid to the dead vehicle at t+2s. PASS = restart==1 and stranded==0. stranded>0 = riders orphaned; restart==0 within timeout = watcher failed to re-arm (players stuck on a corpse-vehicle).
- evidence: maps/t2l2.scr:1038-1039 (destroyed/gone watchers restart instead of stranding); global/vehicles_thinkers.scr:298-320 (tank death -> iprintlnbold fail -> replace.scr::missionfailed reload)  | risk: medium - deliberately destroys the ride and triggers a map reload/transition; run last in a batch. Distinguish a clean scripted restart marker from a genuine crash (check CrashDumps + proc exit, since a bad unglue-on-NULL can fault).


## Objectives HUD + mission progression + map transitions

### [P1 / full] Objective fan-out to ALL 4 players (per-player HUD broadcast)
- catches: Off-by-one / $player[1]-only regressions in the per-player objective send loop, or a stuck sender-latch, that drop the objective on player 2/3/4's HUD (a 4-player-only defect invisible in solo/2p).
- drive: New probe objtest_run in coop_mod/coop_selftest.scr, gated seta coop_objtest 1, launched from main.scr::main alongside the other selftests (main.scr:170-183). Boot the harness with 4 connected+alive players. Probe: waitthread selftest_findPlayer 90, then call `thread global/objectives.scr::add_objectives 1 2 "Regroup with the squad at the church"` and `thread global/objectives.scr::current_objectives 1`; wait 3s (lets coop_objectivesSendPlayer drain its per-player queue). Then loop `for(i=1;i<=$player.size;i++)` and read the SERVER-SIDE mirror the sender writes after a successful send: $player[i].flags["objectiveStatus1"] (global/objectives.scr:553) and .flags["objectiveText1"].
- verify: Per player emit `^~^~^ OBJTEST idx=1 player=<i> name=<netname> status=<flags.objectiveStatus1> textok=<1 if flags.objectiveText1 matches>`, then a summary `^~^~^ OBJTEST done sent=<count status==2> total=<$player.size>`. PASS = sent==total==4 and every player status==2. FAIL = any player status!=2 (dropped/stuck-latch) or textok=0.
- evidence: global/objectives.scr:399-404 (coop_objectivesSendAll loops 1..$player.size), :436-616 (coop_objectivesSendPlayer per-player), :553-554 (sets entity.flags[objectiveStatusN/objectiveTextN after send)  | risk: Low. Server-side only; writes one fake objective to a live map's HUD (cosmetic, wiped on transition). No transition.

### [P1 / full] Sender-latch stuck-1 self-heal (coopObjectivesSending)
- catches: The bug-class where a sender thread dies mid-run and leaves entity.flags[coopObjectivesSending]=1 latched forever, silently swallowing every later objective push to that player (visible symptom: previous mission's objectives frozen on one client). Verifies the 10s take-over self-heal path.
- drive: Extend objtest_run (coop_objtest 1). For $player[2]: force the failure state `$player[2].flags[coopObjectivesSending]=1` and `$player[2].flags[coopObjectivesSendingAt]=level.time-15` (older than the 10s window). Then push a new objective `add_objectives 2 2 "Destroy the fuel depot"`; wait 3s and read $player[2].flags[objectiveStatus2].
- verify: Emit `^~^~^ OBJLATCH player=2 before_latch=1 status2=<flags.objectiveStatus2> healed=<1 if status2==2>`. PASS = healed=1 (take-over processed the queue despite the pre-set latch). FAIL = status2 stays unset/!=2 (latch permanently blocked the push).
- evidence: global/objectives.scr:493-507 (Sending latch + 10s stale take-over), :502-503 (age check)  | risk: Low. Manipulates one player's flag dict on a live map; no transition.

### [P1 / full] Completed-objective idempotency (double-complete + can't-re-current)
- catches: bug-818 class for OBJECTIVES: two players' triggers completing the same objective in one frame must set $world.flags once, pay XP once (not per trigger), and a completed objective must never be re-selectable as the current/yellow objective.
- drive: Probe objidem_run (reuse coop_objtest 1). Same frame, no wait between, fire the completion twice: `thread global/objectives.scr::add_objectives 3 3 "Secure the courtyard"` TWICE. Wait 2s. Then attempt to re-activate it as current: `thread global/objectives.scr::current_objectives 3`. Read: $world.flags["objectiveComplete3"], level.coop_xp_objPaid[3], level.coopCurrentObjective.
- verify: Emit `^~^~^ OBJIDEM complete_flag=<$world.flags.objectiveComplete3> xp_paid=<level.coop_xp_objPaid[3]> current=<level.coopCurrentObjective>`. PASS = complete_flag=1 AND xp_paid=1 (one-shot held, not 2) AND current!=3 (the completed-guard blocked re-current). FAIL = xp double-pay, or current==3.
- evidence: global/objectives.scr:46-56 ($world.flags + threaded xp_primary_done on status 3), :227-232 (completed objective can't become current), xp.scr:711-718 (coop_xp_objPaid[index] one-shot guard)  | risk: Low. No transition; only sets a fake obj complete + XP on a throwaway index.

### [P1 / partial] missioncomplete double-fire idempotency (bug-818 end-trigger)
- catches: An end trigger touched by two players the same frame (or a mission-end threaded twice) running main() twice -> double bsptransition tearing the level down mid-debrief / racing the transition. Verifies the level.coop_missionCompleteRan guard admits exactly one completion.
- drive: New probe mcidem_run gated seta coop_mcidem 1 (+ reuse coop_wintest_next/_bsp). After settle, fire the real path TWICE same frame: `thread global/missioncomplete.scr::main <next> <bsp> 1` immediately followed by a second identical thread. Add a gated marker at coop_mod/missioncomplete.scr:20 (right after the guard sets the flag) `if(getcvar("coop_mcidem")=="1"){ println("^~^~^ MCIDEM pass=1") }` so only invocations that clear the guard print.
- verify: Harness greps for `^~^~^ MCIDEM pass=1` - must appear EXACTLY ONCE across the boot; then confirm the wired next map boots ONCE (single map-load banner, no second transition, no CrashDump). PASS = one MCIDEM + one clean transition. FAIL = two MCIDEM lines, or a crash/double-load.
- evidence: coop_mod/missioncomplete.scr:19-20 (level.coop_missionCompleteRan guard), :62-71 (debrief skip + bsptransition), xp.scr:898-899 (xp_summary singleton bug-804)  | risk: Medium. Actually transitions the level (real bsptransition). Run last per map. Requires a one-line gated marker edit to missioncomplete.scr.

### [P2 / partial] Destroyable-objective failsafe fires on non-plant kill
- catches: Plant-to-destroy objectives (nebelwerfers/flak88/cannons/AA guns) stalling the mission forever when the target dies to a bombing run/tank shell/grenade instead of a planted charge - verifies coop_watchDestroyable fires the box BlowUp and the canonical demolition setthread (objective advance) runs.
- drive: New probe failsafetest_run gated seta coop_failsafetest 1, run on a failsafe map (t2l1 nebelwerfers, or e2l1 aaguns, or e3l2 cannons). Probe waits for map init (~15s), records level.coopObjectiveN for the gun objective, then bypasses the plant path and kills the target directly: clear its guard (`$nebel1 nodamage 0`) and `$nebel1 kill` (or deal lethal damage). Wait 2s (watcher grace is 0.3s). Read the objective status var and whether $nebel1_throbbox still exists.
- verify: Emit `^~^~^ FAILSAFE map=<map> target=nebel1 killed=1 box_gone=<1 if $nebel1_throbbox==NULL> obj_before=<n> obj_after=<level.coopObjectiveX>`. PASS = box_gone=1 AND obj_after advanced (2->3 or the wreck setthread ran). FAIL = box still present / objective status unchanged (failsafe never fired -> mission-stall class).
- evidence: coop_mod/main.scr:1833-1842 (coop_watchDestroyable: waits isalive, 0.3s grace, box BlowUp), maps/t2l1.scr:154-157 (nebel watchers), maps/e2l1/aaguns.scr:37-42, maps/e3l2.scr:171-173  | risk: Medium. Map-specific; destructively skips that map's intended plant flow (disrupts normal progression on that boot). Don't chain with the win-test on the same map.

### [P2 / full] coop_objWrap word-wrap unit test
- catches: Off-by-one / boundary regressions in the objectives word-wrap (long text cut off, rows overlapping, an over-long single word losing chars, or the empty-string path throwing). Pure-function correctness that underpins both the URC and ihuddraw panels.
- drive: New probe wraptest_run gated seta coop_wraptest 1 (no players needed - pure function). Call coop_mod/objectives.scr::coop_objWrap with fixed vectors: (a) a 120-char sentence at maxc=55, (b) a string whose word lands exactly on the boundary, (c) a single 70-char token, (d) "" empty. For each, read the returned array size and each line's .size.
- verify: Per case emit `^~^~^ WRAP case=<a..d> lines=<n> maxlen=<max line .size> over=<1 if any line >maxc AND not a single-word overflow>`. PASS = case a lines>=3 & maxlen<=55; case b splits cleanly; case c returns 1 line (documented overflow); case d returns 1 line == "" (no crash). FAIL = over=1 on multi-word input, or a Script Error line between the WRAP markers.
- evidence: coop_mod/objectives.scr:318-357 (coop_objWrap: word boundary split, single-word overflow at :341, empty fallback :354-356), also consumed by global/objectives.scr:586-598  | risk: None. Pure server-side computation, no players, no HUD, no transition.

### [P2 / partial] Side-objective (officer/radio) push + late-join repush to all 4
- catches: The coop_so1/coop_so2 secondary lines missing on player 3/4, or the late-join/respawn repush not re-seeding a player that connected after the reassert window - and the no-boss gating wrongly leaving a permanently-ACTIVE officer line.
- drive: Probe sideobjtest_run gated seta coop_sideobjtest 1 on an officer map. Force registration: `thread coop_mod/objectives.scr::coop_obj_register`; wait 2s. Instrument coop_obj_push to emit a gated push-count when coop_sideobjtest==1 (it already loops 1..$player.size at objectives.scr:46-51). Then simulate late-join repush: `waitthread coop_mod/objectives.scr::coop_obj_repush_player $player[4]` and confirm it wasn't gated off.
- verify: Emit `^~^~^ SIDEOBJ reg=<level.coop_sideObjReg> pushed=<players looped> total=<$player.size> repush4=<1 if repush ran for p4>`. PASS = reg=1, pushed==total==4, repush4=1 on an officer map; and on a no-boss map (t2l2) reg stays 0 (gated off). FAIL = pushed<total, or repush gated when side objs were registered.
- evidence: coop_mod/objectives.scr:32-52 (coop_obj_push loops all players), :88-107 (coop_obj_repush_player, gated on coop_sideObjReg + officer existence), :145-171 (register gating), global/objectives.scr:735-737 (late-join replay calls repush)  | risk: Low. Server-side; sets fake side-obj HUD lines (cosmetic, wiped on transition). Client-side coop_soN value not readable (no rcon) - verifies the push-loop coverage + gating, not the pixel.

### [P3 / partial] Stale-objective reconcile across map transition
- catches: A previous mission's objective (e.g. m5l1b's coop_o4) lingering on the next map that uses fewer slots - verifies coop_obj_reconcile hides every slot the CURRENT map isn't actively using, per (re)spawn, without hiding a live objective.
- drive: Piggyback on the maptest patrol: on each map, after the reconcile's 3 passes (~12s in), a gated snapshot (coop_maptest 2 already teleports/respawns players, which threads coop_obj_reconcile from player.scr:1103) reads level.coopObjectiveN for all 8 slots and the count that are status>=2 with non-empty text. Emit at map-settle and again after a forced respawn.
- verify: Emit `^~^~^ OBJRECON map=<map> active=<count status>=2 & text!=""> slots=<s1..s8 statuses>`. Cross-map assertion in the harness: the active-objective set on map N+1 must equal that map's own add_objectives calls - a slot that is status>=2 on N+1 but was NEVER set by N+1's script (still carrying N's text) flags the reconcile regression. PASS = no slot active on a map that didn't set it. FAIL = a leftover active slot survives all 3 reconcile passes.
- evidence: coop_mod/objectives.scr:274-304 (coop_obj_reconcile 3 passes hide slots status<=1), player.scr:1103 (threaded per spawn). Note: reconcile hides CLIENT coop_oN cvars (unreadable w/o rcon); test asserts on the server-side level.coopObjectiveN mirror instead.  | risk: Low. Read-only snapshot folded into the existing patrol; no extra transition.


## Level triggers / gags / scripted scenes / event bus (coop_mod/eventsystem.scr, replace.scr shims, per-map gags/, trigger-tour probe)

### [P1 / full] Trigger-tour Script-Error attribution fence (4P, per-map)
- catches: Gags/scripted-scene threads that THROW under coop when their trigger volume fires: the classic single-entity $player array-cast (fixed piecemeal in gags/t2l1_tank.scr:50, gags/t3l1_enemyspawn.scr header - regressions recur), NULL-deref after delete (gags/t3l1_enemyspawn.scr:73->80 $santa), and command-applied-to-NULL. Today the trigger tour teleports through volumes but has NO error attribution, so a Script Error can't be tied to the trigger that produced it.
- drive: Boot listen server on target map with 4 connected players; seta coop_maptest 2, coop_maptest_triggers 1, coop_dbno 0. maptest_phase2.scr::coop_maptest_phase2_tick already resolves getTriggerWaypoints (maptest_waypoints.scr:42) and teleports $player[1] through each center. ADD a fence probe line at maptest_phase2.scr:189 (alongside MAPTEST2_WAYPOINT) that also emits the trigger center and the engine's running Script-Error count so the harness can diff per-waypoint. Dwell coop_maptest_dwell_wpt>=6 so the touch registers on the next server think.
- verify: New marker: '^~^~^ TRIGTOUR_FENCE <map> wp=<i> center=<x y z>'. Harness counts 'Script Error' log lines appearing in the window between fence i and fence i+1; PASS = 0 for every window, FAIL = any window with >=1 error (attribute it to the trigger at wp=i). Cross-check the tour completed with the existing MAPTEST2_OBSERVE <map>.
- evidence: coop_mod/maptest_phase2.scr:109-116,184,189; coop_mod/maptest_waypoints.scr:42; gags/t2l1_tank.scr:50; gags/t3l1_enemyspawn.scr:73,80  | risk: Low. Server-side origin teleport only; god/high-health already set (maptest_phase2.scr:86-89). Excludes changelevel/hurt/kill triggers by construction (maptest_waypoints.scr:33-38).

### [P1 / partial] Same-frame double-trip idempotency (2 players, one trigger)
- catches: Trigger-gated gags that assume a SINGLE toucher and mis-handle two players entering the same volume the same frame: one-shot waves spawned twice (double global/ai.scr::spawnset -> ~2x enemies), trigger_once re-firing, or getToucher/istouching returning one player while the gag body still single-casts $player. This is the exact 4P contention class the $player->$player[1] sweep (bug-1032) was fixed for, but firing PATH not just reference.
- drive: coop_maptest 2 + coop_maptest_triggers 1. Add a probe branch that, for a chosen subset of waypoints, sets BOTH $player[1].origin AND $player[2].origin to the same trigger center with NO wait between the two assignments (same server frame), then dwells. Requires 2+ connected (guard: only run when $player.size>=2, else skip - avoids the array-cast throw).
- verify: Marker '^~^~^ DBLTRIP <map> wp=<i> total_pre=<n> total_post=<n>' using level.coop_actorArray["german"].size (same source as coop_maptest_aistate, maptest_phase2.scr:293-304) sampled just before and ~4s after the double-trip. PASS = single expected spawn delta and no Script Error in the window; FAIL = doubled delta (wave fired twice) or any error.
- evidence: coop_mod/maptest_phase2.scr:184,293-304; coop_mod/replace.scr:788-888 (istouching/getToucher return first match only); gags/t3l1_enemyspawn.scr:91,141 (spawnset waves)  | risk: Low. Generic AISTATE-delta oracle can't distinguish an intended per-player spawn from a bug; flag ambiguous deltas for human confirm rather than hard-fail.

### [P1 / full] Event-bus N-subscriber fan-out release (isolated probe)
- catches: eventsystem.scr correctness under many simultaneous subscribers - the real 4P case where all 4 players run 'waitthread game.waittill2 playerdeath' plus scene waiters on one $EventSlave. doEvent fires 'trigger $EventSlave' ONCE (:34) to wake every waiter; each released waiter re-threads clearEvent (:24) which rebuilds level.coop_eventNameList (:40-67). Bugs: a waiter that never releases (event fired once but only 1 of N woke), a double-release, or clearEvent re-entrancy corrupting the name list so a later event can't register.
- drive: New gated probe coop_evttest 1 in coop_selftest.scr: spawn K counted threads each doing 'waitthread game.waittill testEvt' and incrementing a level counter on return; wait a beat so all K are parked; fire 'thread game.event testEvt' ONCE; wait; read the released counter. Run for K=1,2,4,8. Fully synthetic - no map timing, no client input.
- verify: Marker '^~^~^ EVTBUS event=testEvt waiters=<K> released=<R> namelist=<size>'. PASS = R==K on a single doEvent AND namelist returns to 0 (fully cleared) AND a subsequent register/fire of a second event also releases; FAIL = R<K (stuck waiter), R>K (double), or namelist not draining.
- evidence: coop_mod/eventsystem.scr:15-25,29-36,40-67,127-152; coop_mod/variables.scr:30-32 (game.waittill/event/waittill2 aliases)  | risk: Low. Self-contained; leaves no world state. If a waiter hangs, bound the probe with a timeout and emit released=<partial> rather than blocking the boot.

### [P2 / partial] Event-bus integration on a live scene (e2l1 glider, 4P)
- catches: Real scripted-scene fan-out: e2l1/gliderride.scr fires gliderDetached/gliderDescentBegun/gliderTakingFire/gliderPilotShot/gliderRideOver/gliderCrashOver through the bus while every connected player parks a game.waittill2 playerdeath waiter (:839). Catches a scene phase whose waiter never wakes (ride never hands control back), or the per-player death waiter waking spuriously on a non-death event and mis-driving player handling.
- drive: Boot e2l1 with 4 players, let the scripted glider intro run to completion (no input needed - it's cinematic). Instrument each 'thread game.event <phase>' and each releasing 'waittill' in gliderride.scr with a paired '^~^~^' marker (fire vs released) behind a coop_maptest/coop_scenetrace gate so it is inert for players.
- verify: Markers '^~^~^ SCENEFIRE e2l1 phase=<name>' and '^~^~^ SCENEREL e2l1 phase=<name> t=<sec>'. PASS = every fired phase has a matching release within a bound, and MAPTEST2/gliderRideOver reached; FAIL = a fired phase with no release (deadlock) or a Script Error during the sequence.
- evidence: hzm-mohaa-coop-mod/maps/e2l1/gliderride.scr:561,598-599,611,839,954,971,1051; coop_mod/eventsystem.scr:127-152  | risk: Medium. Scene timing varies; use generous per-phase bounds. Long cinematic (~minutes) makes this a soak-style case, not a fast gate.

### [P2 / full] replace.scr shim guard contract fuzz (4P states)
- catches: Regressions in the single->array cast guards that are the #1 coop crash class. istouching/isinside/getToucher throw 'parameter was ARRAY' on a bare $player (replace.scr:800,835,870) and must return 0/NULL on NIL/NULL; player_closestTo/anyValid/anyCanBeSeen must SKIP spectator/dead/noclip players (:217,362,399,446). A gag passing the wrong shape or a dead/spectating player among the 4 is exactly what breaks scenes.
- drive: New gated probe coop_shimtest 1 in coop_selftest.scr calling each shim with: (a) a valid single trigger ent, (b) NULL, (c) NIL, (d) a 2+ entity array, and with the 4 connected players cycled through alive / dead / spectator / noclip via server-side health/dmteam sets. Assert the guard returns rather than errors, and that closest/any selectors never return a spectator/dead player.
- verify: Marker '^~^~^ SHIMTEST fn=<name> case=<a-d> ret=<0|1|NULL|ent> err=<0|1>'. PASS = err=0 for every case AND selector cases never return an excluded player; FAIL = any err=1 (guard threw) or a spectator/dead/noclip player selected.
- evidence: coop_mod/replace.scr:188-236,333-371,375-413,788-818,820-888  | risk: Low. Mutating a real player's dmteam/health server-side is reversible; restore after each case. Keep the array arg a throwaway spawned pair, not $player, to avoid a genuine cast throw.

### [P2 / partial] Scene VO length-table coverage (say_wait/playsound_wait)
- catches: Scripted-scene desync from unregistered dialogue: say_wait/playsound_wait derive their wait from level.coop_soundLength[lang][alias]; a missing alias prints a WARNING and returns EARLY (replace.scr:1010-1018,1070-1078), so the scene advances before the VO finishes - broken pacing, overlapping lines, or a wait-for-saydone that never gets its beat. Silent in normal play until someone notices timing is off.
- drive: New gated probe coop_saylentest 1 that, for the current map, walks the dialogue aliases the map's gags/scene reference (or the full alias set) and looks each up in level.coop_soundLength[level.coop_language]; count misses. Run across the map rotation via the existing transition path.
- verify: Marker '^~^~^ SAYLEN map=<m> lang=<l> checked=<n> missing=<n> firstmiss=<alias>'. PASS = missing=0; FAIL = missing>0 (each is a scene that will run its say_wait on the fallback early-return path).
- evidence: coop_mod/replace.scr:994-1027,1052-1086; coop_mod/sound.scr (length table populated per language)  | risk: None (read-only). Needs a per-map alias inventory to be meaningful; without it, a full-table sweep over-reports aliases the map never uses.

### [P2 / partial] Alarm trigger -> reinforcement path (4P)
- catches: Alarm/ambush trigger scripts that strand or throw: disable_ai+runto+movedone reinforcement chains that reference $player single-cast, or an alarm that never spawns its wave. maptest_phase2.scr already falls back to $alarmspawner origins (:125-128), so alarm volumes are tourable; this adds the fired/errored oracle.
- drive: coop_maptest 2 + coop_maptest_triggers 1 on alarm-bearing maps (e/t series). Teleport a player onto the alarm trigger center, dwell, and sample the german actor array before/after to confirm the reinforcement wave actually spawned.
- verify: Marker '^~^~^ ALARMTEST <map> wp=<i> alive_pre=<n> alive_post=<n> err=<0|1>'. PASS = alive_post>alive_pre (wave arrived) and err=0; FAIL = no delta (alarm dead) or a Script Error in the window.
- evidence: coop_mod/maptest_phase2.scr:125-128,293-304; MEMORY alarm_path_reference (disable_ai+runto+movedone pattern)  | risk: Low. Some alarms need line-of-sight or a specific approach vector a raw teleport won't satisfy - a no-delta result may be a false negative; flag for human spot-check.

### [P3 / partial] Late-join during an active scripted scene
- catches: Scene/gag threads that snapshot $player.size at scene start and index out of range or skip per-player setup when a 4th client connects mid-scene (music replay via level.coop_musicCommand, objectives push, per-player death waiters). A scripted scene running when someone joins is a known coop soft spot.
- drive: Boot with 3 players, start a scene (glider intro on e2l1, or a tank gag on t2l1), then connect the 4th client WHILE the scene is mid-run (harness times the connect against a SCENEFIRE marker). Observe for Script Error and whether the late joiner receives scene state.
- verify: Marker '^~^~^ LATEJOIN map=<m> scene=<x> size_before=<3> size_after=<4> err=<0|1>' plus confirming the joiner's music/objective replay lines appear. PASS = err=0 and joiner state applied; FAIL = Script Error, array-index error, or missing joiner setup.
- evidence: coop_mod/replace.scr:553-582,625-651 (level.coop_musicCommand replay for later joiners); maps/e2l1/gliderride.scr:838-839 (per-player scene waiter)  | risk: Medium. Connect timing relative to scene phase is imprecise; a headless client join is automatable but the scene-state oracle is coarse, so treat as a smoke/soak case rather than a strict gate.


## Officer boss waves + reinforcement scaling + AI difficulty (officer.scr / aihandler.scr / director.scr)

### [P1 / full] Wave/guard/battalion count-scaling math across 2P/3P/4P
- catches: Regressions in the single count authority coop_reinf_count: wrong coop_officerBasePc anchor (def 2), broken per-player step (coop_officerSquadPer/coop_officerBattalionPer), cap not applied, or the coop_reinf_pc player-count read/coop_maxPlayerScalar clamp breaking. This is THE scaling contract for guards, squads and battalions.
- drive: New cvar-gated probe officertest_run (coop_officertest 1, mode=scale) in coop_mod/coop_selftest.scr that, once selftest_findPlayer returns, directly evaluates the real function against the live lobby: local.bg=exec coop_mod/officer.scr::coop_reinf_count 2 "coop_officerSquadPer" 1 4 ; local.bt=exec coop_mod/officer.scr::coop_reinf_count 10 "coop_officerBattalionPer" 3 22 ; local.pc=exec coop_mod/officer.scr::coop_reinf_pc. No officer/entity spawn needed - pure math against $player.size. Harness boots the SAME map 3 times at 2, 3, 4 connected clients (and once solo with seta coop_aiScaleTest 0 to check down-scaling: solo battalion must be 10-3=7).
- verify: Emit ^~^~^ OFFICERTEST scale pc=<n> guards=<bg> battalion=<bt> maxscalar=<level.coop_maxPlayerScalar>. Assert across runs: guards 2->2->2->3->4 pattern (base 2 at 2P, +1/player capped 4), battalion strictly increasing 10(2P)<13(3P)<16(4P), solo battalion==7, and both clamp at pc=coop_maxPlayerScalar. Pass = monotonic + matches base+(pc-basePc)*per.
- evidence: officer.scr:1422 coop_reinf_count, officer.scr:1404 coop_reinf_pc, officer.scr:577 guard count call, officer.scr:1684 battalion count call  | risk: None - pure read, no spawns, no state mutation.

### [P1 / full] Enemy per-spawn clone hardcap holds at 4 players
- catches: coop_aiScaleHardCap failing to bound the per-spawn duplicator (aihandler coop_tryDuplicateActor clones every german with 2+ players). If the cap check misfires the german array overruns the entity budget - the exact class that caused the entity-pool-2048 world-stomp crash weekend. Also verifies replicas set coop_isReplica so a clone never clones again (recursion guard).
- drive: Boot 4 clients on a dense combat map (e.g. m3l2 or t2l1) with seta coop_aiScale 1, seta coop_aiScaleChance 100 (force max cloning). Add mode=cap to officertest_run: loop wait 5 for ~60s reading level.coop_actorArray["german"].size and a replica tally (count actors whose flags["coop_isReplica"]==1).
- verify: Emit ^~^~^ OFFICERTEST cap german=<size> replicas=<r> hardcap=<level.coop_aiScaleHardCap> each tick. PASS = german never exceeds coop_aiScaleHardCap on any tick AND no 'Script Error' / crash-dump / proc-exit during the run. Cross-check the harness memory sample stays flat-ish (no unbounded growth).
- evidence: aihandler.scr:146 coop_tryDuplicateActor, aihandler.scr:198 hardcap break in clone loop, aihandler.scr:221 coop_isReplica recursion guard, officer.scr:1438 hardcap re-check in coop_reinf_count  | risk: Forcing chance=100 on 4P is a stress load - that is the point; watch for legit overflow (a real find) vs harness OOM.

### [P1 / partial] Wave troops excluded from generic scalers (no double-dip)
- catches: Officer wave/battalion/guard actors must carry coop_waveActor=1 (skips the generic per-spawn cloner) and coop_actorStopAccuracy=1 (skips the generic aihandler accuracy curve) so count and accuracy are scaled ONCE by the officer system, not twice. A regression here = battalions silently doubled by the cloner or wave accuracy compounded past intent.
- drive: mode=wave: probe spawns a throwaway officer stand-in (spawn models/human/german_waffenss_officer.tik; set level.coop_officer + level.coop_officer_alive=1 + level.coop_officer_reinf_zone near players) then exec coop_mod/officer.scr::coop_spawn_battalion. wait 3, then walk level.coop_battalion[] and level.coop_actorArray["german"].
- verify: Emit ^~^~^ OFFICERTEST wave battalion_n=<level.coop_battalion_n> flagged=<count with coop_waveActor==1> replicas_among_wave=<count coop_isReplica==1> stopacc=<count coop_actorStopAccuracy==1>. PASS = flagged==battalion_n, replicas_among_wave==0, stopacc==battalion_n. Fail if any wave trooper got cloned or lacks the accuracy-skip flag.
- evidence: officer.scr:1460 coop_reinf_scale sets coop_waveActor+coop_actorStopAccuracy, aihandler.scr:164-165 cloner excludes coop_actorStopPainHandler/coop_waveActor, aihandler.scr:423 accuracy calc gated on coop_actorStopAccuracy  | risk: Spawning a fake officer + battalion mutates level state; run on a throwaway boot and let the harness restart after. Needs a map with navmesh near the spawn.

### [P1 / partial] Officer heal/retreat completes when damaged and unseen
- catches: The coop_officer_medkit_retreat path: officer under 40% HP (<480) and out of sight retreats to a health post, heals back to 1200, and re-arms thresholds. Bug classes: coop_officer_busy latch deadlock (never heals again), stuck pathing (runto never reaches, infinite loop), or heal firing while players watch. Also validates the retreat_monitor threshold (health<100 -> emergency).
- drive: mode=heal: on an officer map, let coop_officer_init spawn the boss (or force via the fake-officer setup). Probe sets level.coop_officer.health=400 (below the 480 gate), then server-teleports ALL $player far away (each .origin = boss.origin + (6000 0 0)) so coop_officer_lastseen ages past the unseen threshold. Poll level.coop_officer.health for ~40s.
- verify: Existing coop_officer_hp_logger already prints HP; add ^~^~^ OFFICERTEST heal startHP=400 busy=<level.coop_officer_busy> reachedPost=<0/1> finalHP=<hp> ticks=<n>. PASS = finalHP returns to 1200 within the window AND level.coop_officer_busy resets to 0 afterward (re-armable). FAIL = HP stuck at 400 (deadlock) or busy latched at 1.
- evidence: officer.scr:837 coop_officer_medkit_retreat, officer.scr:839 busy latch, officer.scr:936 runto + stall-reissue loop, officer.scr:951 heal loop, officer.scr:960 restore 1200, officer.scr:820 coop_officer_retreat_monitor  | risk: Requires a map that actually has a health entity for the scan (officer.scr:903 prints 'no health entity - retreat skipped'); pick a map known to have one, else the test is inconclusive not failing.

### [P1 / full] DDA reacts: near-wipe valley + steamroll ramp
- catches: director.scr EI runaway or frozen levers. Two behaviors: (a) near-wipe (all players down/dead, upN==0) must SNAP EI to floor, open a relax valley, force coop_officerWaveCooldown up and coop_aiScaleChance 0; (b) sustained steamroll (high kills, full HP, no downs) must ramp EI up and dial coop_officerWaveCooldown down + coop_aiScaleChance up. Also checks the asymmetric ease-fast/ramp-slow guard and campaign-persistent coop_ddaSkill write-back.
- drive: Boot with seta coop_ddaEnabled 1, seta coop_ddaDebug 1, seta coop_ddaCadence 5. Steamroll: mode=dda_up probe increments level.coop_dda_kills by ~10 every 5s for 60s while keeping players full HP (no DBNO). Wipe: mode=dda_wipe drives all players into DBNO via coop_mod/dbno.scr::dbno_enter so upN==0 for one cadence tick.
- verify: director already emits 'DDA: ei=<f> p=<f> downs60=<n> kpm=<n> cd=<n> sq=<n> bt=<n> ch=<n>' (director.scr:203) and 'DDA: WIPE -> ei floor + valley' (director.scr:124). Harness greps those. PASS steamroll = ei rises across ticks and cd (coop_officerWaveCooldown) trends down while ch (coop_aiScaleChance) trends up. PASS wipe = a WIPE line appears, next getcvar snapshot in boot cfg shows coop_aiScaleChance 0 + coop_officerWaveCooldown ~= coop_ddaValley. Add ^~^~^ OFFICERTEST dda phase=<up/wipe> ei=<f> cd=<cvar> ch=<cvar> to mirror for parsing (no rcon).
- evidence: director.scr:116 wipe snap+valley, director.scr:174-200 ramp/ease + lever dials, director.scr:189 coop_ddaSkill writeback, director.scr:40 counters fed by aihandler.scr:910 addKillToPlayer  | risk: DDA ship-default is OFF; test only meaningful with coop_ddaEnabled 1. Forcing kills via a level counter is synthetic - keep it isolated to the dda counter, not real spawns.

### [P2 / partial] Wave-cooldown gate rejects rapid re-calls
- catches: coop_call_reinforcements funnels EVERY wave request (HP thresholds, bodyguard deaths, brain) through the coop_officerWaveCooldown gate so the officer cannot spam waves as his HP drains. Regression = wave flooding (perf + entity budget).
- drive: mode=cooldown: with an officer alive, probe calls thread coop_mod/officer.scr::coop_call_reinforcements twice back-to-back (wait 0.5 between), with seta coop_officerWaveCooldown 30. Record level.coop_wave_count before and after each call.
- verify: Emit ^~^~^ OFFICERTEST cooldown call1_wave_count=<n> call2_wave_count=<m> lastWaveTime=<t>. PASS = second call does NOT increase level.coop_officer_lastWaveTime / does not dispatch (the 'reinforcements are not ready yet' iprintlnbold fires; grep it). FAIL = both calls dispatch a wave within the cooldown window.
- evidence: officer.scr:1291 cooldown read, officer.scr:1295 gate check, officer.scr:1301 lastWaveTime stamp  | risk: Actually dispatches one real wave (spawns actors) - low, but run on a throwaway boot.

### [P2 / partial] Airstrike gating: never Stuka/Artillery on interior or unseen
- catches: Top-down wave types 4 (Stuka) and 5 (Artillery) must reroll to on-foot infantry when level.coop_officer_no_airstrikes==1 (interior maps) OR when no player is both visible to the officer AND under open sky. Regression = bombs dropping through a sub-pen roof / striking a player the officer cannot see.
- drive: mode=airstrike: on an interior officer map (one with coop_officer_no_airstrikes set in officer_positions.scr), probe loops ~30 times calling exec coop_mod/officer.scr::coop_wave_allowed and forcing the roll path - or simpler, temporarily seta coop_officerWaveCooldown 0 and fire coop_call_reinforcements repeatedly, tallying which wave-type iprintlnbold lines appear.
- verify: Emit ^~^~^ OFFICERTEST airstrike map=<name> no_airstrikes=<flag> stuka=<count> arty=<count> reroll_to_infantry=<count>. PASS on interior map = stuka==0 AND arty==0. Also parse the existing 'called in a Stuka'/'Artillery barrage' iprintlnbold lines - they must never appear on the no-airstrike map.
- evidence: officer.scr:1351 strike gating, officer.scr:1352 coop_officer_no_airstrikes reroll, officer.scr:1356 visible-outdoor-player check, officer.scr:1381-1388 dispatch  | risk: Fires multiple real waves; entity churn on a stress boot. Pick a map with a genuinely-set no-airstrike flag or the test proves nothing.

### [P2 / partial] Reinforcements actually path and engage after spawn
- catches: Waves that spawn but never path/attack (idle troopers) - the 'missing battalion' / stuck-spawn class, and forceactivate/attackplayer not taking. Verifies coop_spread_pos cap keeps them reachable and coop_battalion_march/attack drives them onto players.
- drive: mode=wave (reuse the battalion spawn from test 3) at 4P, then reuse the existing AISTATE snapshot loop (coop_maptest_aistate) or emit our own: after wait 8, count level.coop_actorArray["german"] entries that are isAlive and have .enemy != NULL.
- verify: Emit ^~^~^ OFFICERTEST engage spawned=<battalion_n> alive=<a> targeting=<t>. PASS = alive >= ~80% of spawned (few stuck/void deaths) AND targeting > 0 within 10s (they acquired players). FAIL = alive much less than spawned (stuck-in-wall deaths) or targeting==0 (spawned but inert).
- evidence: officer.scr:1499 coop_spread_pos radius cap, officer.scr:1719 forceactivate, officer.scr:1721 coop_battalion_march, maptest_phase2.scr:304 AISTATE targeting model to reuse  | risk: Needs players positioned in the navmesh near the spawn anchor; on a bad anchor a low score may be map-geometry, not a code regression - corroborate across 2 maps.

### [P2 / full] Late-join raises subsequent wave size (live player-count read)
- catches: coop_reinf_count/coop_reinf_pc read $player.size LIVE per wave, so a client joining mid-map must enlarge the NEXT wave. Exercises the 4-player array dimension + the 'cast array to listener' hazard when player count changes. Regression = wave size latched at map-load count.
- drive: mode=scale but staged: boot 2 clients, probe evaluates coop_reinf_count (battalion) and emits it; harness connects a 3rd then 4th client; probe re-evaluates after each join (poll $player.size change) and re-emits.
- verify: Emit ^~^~^ OFFICERTEST latejoin pc=<n> battalion=<bt> at each stage. PASS = battalion value increases as pc goes 2->3->4 with NO 'cast array to listener' or other Script Error line between joins. FAIL = value frozen at the 2P number, or an array-cast error when player count shifts.
- evidence: officer.scr:1406 live $player.size read, officer.scr:1425 coop_reinf_pc call per wave, CLAUDE.md note: single-entity $player with 2+ connected throws cast-array  | risk: None beyond normal join churn; pure read.

### [P3 / full] Accuracy scaler divide-by-zero guard under coop_maxPlayerScalar edge
- catches: aihandler calculateAccuracy divides the accuracy range by level.coop_maxPlayerScalar (line 457). If coop_maxPlayerScalar is set to 0 the accuracyMult>1 guard is the ONLY thing preventing a divide-by-zero Script Error; a regression that reorders those checks would crash accuracy calc on every enemy.
- drive: mode=acc: boot 4P with seta coop_maxPlayerScalar 0 (and a second run at 1) on a combat map with living germans; let updateEnemyTeamDifficulty run over the array.
- verify: Emit ^~^~^ OFFICERTEST acc maxscalar=<n> enemies=<size> errors=<parsed>. PASS = zero 'Script Error' / no division fault in qconsole.log across the enemy set at maxscalar 0 and 1. FAIL = a script error referencing calculateAccuracy.
- evidence: aihandler.scr:445 maxPlayerScalar<0 guard, aihandler.scr:449 clamp, aihandler.scr:457 division by coop_maxPlayerScalar  | risk: Edge cvar value; harmless, revert after boot.

### [P3 / full] Officer init idempotent - no duplicate boss
- catches: coop_officer_init aborts if level.coop_officer already exists and coop_officer_alive==1. A regression (e.g. deferred-policy release racing a re-init, or build-mode suppress leaving stale flags) could spawn two officers -> two eagle icons, double waves, double music.
- drive: mode=doubleinit: after the officer has spawned, probe calls thread coop_mod/officer.scr::coop_officer_init a second time.
- verify: Emit ^~^~^ OFFICERTEST doubleinit officer_alive=<flag> officer_ent=<level.coop_officer valid?>. Also grep the existing 'OFFICER: init aborted - officer already exists' iprintlnbold. PASS = the abort line fires and level.coop_officer is unchanged (same entity). FAIL = a second 'OFFICER SPAWNED HP' line appears.
- evidence: officer.scr:129 init idempotency guard, officer.scr:544 level.coop_officer set, officer.scr:618 coop_officer_buildSuppress flag reset  | risk: Minimal - the guard should no-op; if it doesn't, that IS the finding.


## Key items + reward items + disguises + loadout inventory (coop_mod/keyitems.scr, itemhandler.scr, loadout.scr, loadoutpick.scr, loadoutroster.scr, officer.scr reward HUD, dbno.scr/medkit.scr revive-regive)

### [P1 / partial] MISSIONITEM persistence matrix (mine detector / papers) across DBNO-revive, death-respawn, spectate-rejoin - per player, 4P
- catches: The exact bug-620 / bug-893 regression class: mission-critical map gear registered in level.coop_missionItems (e1l2 us_w/gr_w_minedetector, papers) silently lost on any inventory rebuild. Every rebuild path runs takeall (itemhandler.scr:708) and only spawnInventory's append (itemhandler.scr:1425-1435) + coop_reissueMissionItems (main.scr:818-844, dbno.scr:696, medkit.scr:388) re-issues it - so a single missed path drops the detector for that player only. 4P dimension: registration is level-global but re-give runs per-player under the shared changeGameType-0 hack, so a race on one player must not strip another's.
- drive: Add probe missiontest_run to coop_mod/coop_selftest.scr gated on coop_missionitemtest 1 (harness seta's it + coop_missionItemDebug 1). Probe: waitthread itemhandler::addMissionItemToEveryone "models/weapons/us_w_minedetector.tik"; scan $player[1..size]; for each alive p emit baseline then run 3 legs sequentially: (a) DBNO - thread dbno.scr::dbno_enter p "chest", poll p.flags["coop_dbno_active"]==1, waitthread dbno.scr::dbno_ai_revive p, wait 3; (b) RESPAWN - p.health=0, poll isAlive false->true (harness watches manageAliveSpawning); (c) SPECTATE - harness stufftexts that client "spectator" then "join allies" (best-effort). After each leg check membership via waitthread itemhandler::playerHasWeapon p <model> (registry) AND reissue count.
- verify: Emit ^~^~^ MITEM p=<netname> stage=<baseline|dbno|respawn|spectate> has=<0|1> reg=<0|1> where reg=level.coop_missionItems.size>0. Also assert existing ^~^~^ REISSUE called player=<netname> n=<>=1> fires on the dbno/respawn legs and one ^~^~^ REISSUE give <model> to <netname> per item. PASS = has=1 for every player at every stage; FAIL = any has=0 or any REISSUE n=0 (registration lost) / missing REISSUE give line (give path skipped).
- evidence: coop_mod/itemhandler.scr:1425-1435 (append), :1583-1594 (addMissionItemToEveryone idempotent), :1876-1885 (playerHasWeapon reads coop_inventoryWeapons); coop_mod/main.scr:818-844 (coop_reissueMissionItems + REISSUE markers); coop_mod/dbno.scr:696; coop_mod/medkit.scr:388  | risk: Downs/kills each player in turn; benign (real revive+respawn paths). Spectate leg depends on client team-switch sticking (server stufftext of team commands is unreliable per session constraints) - mark that one leg needs-human if the spectate stufftext doesn't take.

### [P1 / full] EXACT-AMMO respawn 4P independence (per-player reserve, no cross-wire, no default clobber)
- catches: coop_ammoSnapshotLoop/coop_ammoRestore handing the WRONG player's reserve or falling back to default ammo on respawn - array-index or shared-flag cross-wiring across 4 concurrently-snapshotting players, and the bug-913 pending-gate window where a snapshot tick clobbers the down-moment value or the restore races the async default-ammo give (setammo at itemhandler.scr:2556 vs giveInventory default at :1558).
- drive: Probe exactammotest_run gated on coop_exactammotest 1; harness also seta coop_exactAmmo 1. For each player i (1..size): self setammo rifle (100 + i*10), setammo smg (200 + i*10), setammo pistol (30 + i) - distinct sentinels per player and per class. wait 0.6 (one coop_ammoSnapshotLoop tick at 0.5s cadence, guard self.flags["coop_ammoSaved_valid"]==1). Then kill all: for each p p.health=0. On respawn (poll isAlive), wait 1.2 (past coop_ammoRestore's internal wait 0.5 + assert), read self getammo for each class.
- verify: Emit ^~^~^ EXACTAMMO p=<netname> class=<rifle|smg|pistol> want=<n> got=<n> ok=<0|1> after respawn. PASS = got==want for every player/class (proves per-player restore, no cross-wire, no default overwrite). FAIL = got==map-default (restore skipped/raced) or got==another player's sentinel (index cross-wire).
- evidence: coop_mod/itemhandler.scr:2487-2519 (snapshot loop, per-player self.flags coop_ammoSaved_*), :2529-2561 (restore, pending gate), :2507 (bug-913 gate); coop_mod/player.scr:930 (snapshot start), :973 (restore on respawn)  | risk: Kills all 4 players (they respawn). Requires the HZM engine getammo/setammo events (game.dll 07-09+) - abort-marker if unsupported.

### [P1 / full] changeGameType contention under simultaneous 4P disguise + multi-primary backfill
- catches: The single-instance changeGameType invariant (only one flip at a time; guarded by level.coop_disguisingInProgress and the SP-hack lock) breaking when 4 players hit gametype-flipping paths at once - giveDisguise (itemhandler.scr:919-922), coop_backfillPrimaries (:1347-1362), and coop_reissueMissionItems (main.scr:827-844) all flip to gametype 0 and back. If they interleave, the level can stick at g_gametype 0 (SP) - which silently disables ALL coop gating (level.gametype!=0) - or throw a cast/none error. This is the worst-case whole-mod breakage and is inherently a 4P concurrency test.
- drive: Probe gtcontendtest_run on a disguise-capable map with a 2-primary kit (e.g. m3l2: springfield+thompson+bar, or e1l1 m1_garand+thompson). Simultaneously: thread itemhandler::giveDisguise on all 4 players in the same frame AND kill all 4 (p.health=0) so each respawn fires coop_backfillPrimaries (2+ primaries) in the same window. wait 6 for the storm to settle, then read level.gametype, getcvar g_gametype, level.coop_disguisingInProgress.
- verify: Emit ^~^~^ GTCONTEND gametype=<level.gametype> gcvar=<g_gametype> disguising=<0|1> after settle. Harness greps the whole window for 'Script Error' and 'cast'. PASS = gametype==2 AND gcvar==2 AND disguising==0/false AND zero Script Error/cast lines. FAIL = gametype/gcvar stuck at 0, disguising left 1, or any cast error.
- evidence: coop_mod/itemhandler.scr:919-922 + :898-908 (disguise lock), :1347-1362 (backfill gametype flip), :1288 (SP guard); coop_mod/main.scr:827-844 (reissue flip); CLAUDE.md 'only one instance can run' note on changeGameType  | risk: Deliberately aggressive - if the invariant is broken this can wedge the test server at g_gametype 0. Run last / on a disposable boot; watchdog should crash-detect and restart.

### [P2 / partial] Reward-item single-owner invariant + per-owner HUD gating (binoc & smoke), 4P owner death re-assignment
- catches: level.coop_binoc_owner / level.coop_smoke_player desync so the owner-only HUD (officer.scr:3822, :3864 gate each ihuddraw to one client) leaks to non-owners or two players are simultaneously 'owner', and the drop-on-death watch (coop_binoc_owner_watch :3908, coop_smoke_owner_watch :3962) failing to clear the owner when the holder dies with strikes left.
- drive: Probe rewardtest_run: pick a=$player[1]. Assign binoc: level.coop_binoc_given=1, level.coop_binoc_owner=a, level.coop_binoc_uses=3, a item weapons/coop_binoculars.tik, thread officer::coop_binoc_update_hud a. Emit owner snapshot. Then a.health=0 (owner death) -> coop_binoc_owner_watch should re-drop; poll up to 8s for level.coop_binoc_given==0 && level.coop_binoc_owner==NULL. Repeat the assign/kill cycle for the smoke item (level.coop_smoke_player, coop_paradrop_armed). Re-pickup-across-players is best-effort: teleport $player[2] onto the drop trigger origin and re-check owner==player2.
- verify: Emit ^~^~^ REWARD item=<binoc|smoke> phase=<assigned|afterdeath|repickup> owner=<netname|none> given=<n> uses=<n>. Harness greps the HUD-update window for Script Error/cast (per-owner iterate over $player must not throw). PASS = exactly one owner at 'assigned', owner=none & given=0 at 'afterdeath', no script errors. Actual HUD pixel visibility is not server-readable -> spot-check.
- evidence: coop_mod/officer.scr:3452 & :3486 (owner set), :3817-3841 & :3859-3884 (per-owner HUD gate), :3908-3921 & :3962-3970 (owner-death drop/clear); coop_mod/variables.scr:76-77 (NULL init)  | risk: Kills the owner (respawns). Re-pickup leg relies on a trigger_multiple touch firing from a server origin-set, which is unreliable - flag that leg needs-human.

### [P2 / partial] Heavy weapon (panzerschreck/bazooka) drop-on-DBNO and no-auto-restore-on-revive, 4P recover
- catches: keyitems.scr::coop_key_onDown failing to drop a heavy weapon where a downed player fell (so it is lost with them) OR a revive path silently re-restoring a dropped heavy (duplicate / un-recoverable). Also the removeWeaponFromInventory bookkeeping (keyitems.scr:45) so the registry matches the world drop.
- drive: Probe heavytest_run on a bazooka map (m5l2a) or give the model: p item models/weapons/bazooka.tik + ammo rocket 4. thread dbno.scr::dbno_enter p "chest" (dbno_enter calls coop_key_onDown before takeall). After down: check waitthread itemhandler::playerHasWeapon p <model>==0 and that a dropped script_model / trigger exists near p.origin (probe can scan or set a flag when coop_key_dropWeapon runs). Revive via dbno_ai_revive p; assert playerHasWeapon still 0 (must re-pickup, not auto-restore). Optional: teleport p onto the drop trigger to recover.
- verify: Emit ^~^~^ HEAVY p=<netname> stage=<down> has=<0|1> dropped=<0|1>; ^~^~^ HEAVY p=<netname> stage=<revive> has=<0|1>. PASS = down: has=0 & dropped=1; revive: has=0. FAIL = down has=1 (drop failed) / revive has=1 (silent restore).
- evidence: coop_mod/keyitems.scr:28-50 (coop_key_onDown, heavyModels), :103-160 (drop + re-pickup trigger), :44-45 (drop + removeWeaponFromInventory)  | risk: Downs one player; benign. Re-pickup leg needs a real touch -> needs-human.

### [P2 / full] Disguise per-player apply (4P all disguised) + papers-only weapon lockout (e1l3/e1l4)
- catches: A player left un-disguised while teammates are disguised - per-player has_disguise / coop_hasDisguise / is_disguised desync in giveDisguiseToAll's 4P loop (itemhandler.scr:965-969, retry at :983-990), and the papers-only failproof class leaking a weapon (coop_noWeapon lockout) so a player could draw a gun in front of a guard on e1l3.
- drive: Probe disguisetest_run on a disguise map: waitthread itemhandler::giveDisguiseToAll; wait 2; iterate $player[1..size] emitting per-player flags and whether returnActiveWeapon is a Weapon. On a papers-only map (level.coop_noWeapon set, e1l3): additionally assert every player's returnActiveWeapon is NULL/non-Weapon and self.flags["coop_hasPapers"] is set.
- verify: Emit ^~^~^ DISGUISE p=<netname> has=<0|1> is=<0|1> armed=<0|1> papers=<0|1>. PASS (disguise map) = has=1 for all players; PASS (papers-only) = armed=0 & papers=1 for all. FAIL = any has=0 (un-disguised straggler) or armed=1 on a papers-only map.
- evidence: coop_mod/itemhandler.scr:945-1010 (giveDisguiseToAll, per-player loop + retry), :724-736 (per-spawn disguise/papers), :2087 (givePapersFlag), :711 coop_noWeapon lockout; coop_mod/keyitems.scr:73-89 (papers re-give after revive)  | risk: Low - flag reads + one disguise flip; no kills.

### [P2 / full] Second-primary backfill under concurrent 4P respawn (bug-897 regression sensor)
- catches: The engine team-mode one-primary PickupWeapon rule dropping a 2nd primary when 4 players respawn in the same frame and coop_backfillPrimaries shares the level-global `group` - verifying the param-passing fix (player+kit passed explicitly, itemhandler.scr:1213, :1285) holds under contention and every player's full multi-primary kit is re-issued.
- drive: Harness seta coop_missionItemDebug 1; boot a 2-primary map (m3l2 = springfield+thompson+bar, or e1l1 = m1_garand+thompson). Probe (or harness) kills all 4 players in one frame; each respawn independently fires coop_backfillPrimaries. No new marker needed - leverage the existing KITHOLD/KITBACKFILL instrumentation.
- verify: Grep existing ^~^~^ KITBACKFILL done player=<netname> primaries=<n> - expect exactly 4 lines with 4 DISTINCT netnames, primaries>=2 each. The pre-backfill ^~^~^ KITHOLD player=<netname> ... verdict=ABSENT lines confirm the engine actually dropped the 2nd primary (sensor is live). PASS = 4 distinct KITBACKFILL done, primaries>=2 each; FAIL = fewer than 4, a repeated netname (group race), or primaries<2.
- evidence: coop_mod/itemhandler.scr:1273-1364 (coop_backfillPrimaries, KITHOLD/KITBACKFILL markers), :1437-1453 (per-invocation intendedKit deep-copy), :1212-1213 (param pass to avoid group race)  | risk: Kills all 4; benign. coop_missionItemDebug 1 is dev-only and already inert for players.

### [P2 / partial] Armory picks authoritative on spawn AND respawn for all 4 (no cross-player kit, in-level 2nd primary carried over)
- catches: managePlayerInventory/giveWeaponLoadout handing a player another player's armory picks or reverting to the map-default kit on respawn - the coop_loWeapons/coop_loSlot per-player authority (itemhandler.scr:746, :1196-1199, :1406-1412) and the bug-897 in-level-pickup carry-over (mergeCarryWeapons :1505). 4P: picks are per-player flags; a shared-array bug cross-wires them.
- drive: Probe armorytest_run: synthesize distinct picks server-side per player (set p.flags["coop_loWeapons"], coop_loSlot1/2 to a different roster weapon each, e.g. p1=thompson, p2=bar, p3=springfield, p4=mp40, using loadoutroster ids), set level.coop_loadoutLocked=0. For each p: p.flags["coop_inventoryWeapons"]=NIL; waitthread itemhandler::managePlayerInventory (spawn path). Read back active weapon + registry. Then kill p and re-read on respawn.
- verify: Emit ^~^~^ ARMORY p=<netname> stage=<spawn|respawn> slot1=<want> has1=<0|1> mine=<0|1> where mine=1 if the player's registry contains ONLY their own picks (no teammate's). PASS = has1=1 and mine=1 for every player at both stages. FAIL = has1=0 (default kit won) or mine=0 (cross-wired to another player's picks).
- evidence: coop_mod/itemhandler.scr:739-749 (picks force default path), :1196-1208 (desired = coop_loSlot1/2), :1404-1412 (picks authoritative + mergeCarryWeapons), :1505-1540 (mergeCarryWeapons dedupe); coop_mod/loadoutpick.scr:487 (loadout_isPicked), :197-225 (regive watch)  | risk: Synthesizing picks server-side (rather than via real client coop_loA1..4 cvars) bypasses the client-plumbing leg - it validates the apply/spawn logic but not the wire delivery; note that gap. Kills players for the respawn leg.


## DBNO edge cases + LMS + spawn protection + player lifecycle (coop_mod/dbno.scr, medkit.scr, player.scr, voidguard.scr, main.scr LMS)

### [P1 / full] 4-player simultaneous team-wipe bleedout->death
- catches: bleedout-timer completion + dbno_die kill for 4 CONCURRENT DBNO instances; verifies the per-teammate broadcast loops (dbno_enter 178-187, marker_manager 868-885) and the team-revive monitor's $player scan all behave when every player is down at once (no living reviver, no array cast, shared HUD slots 20/27-35 stay per-self).
- drive: New gated probe dbnowipe_run (coop_dbnowipe 1) in coop_selftest.scr: seta coop_dbnoTimer 4, coop_teamRevive 0; find every alive $player[i]; thread coop_mod/dbno.scr::dbno_enter <p> "chest" on each; emit begin marker with count; poll coop_dbno_active on all.
- verify: Emit '^~^~^ DBNOWIPE begin n=<count>'. PASS = exactly n '^~^~^ DBNO down p=' AND n '^~^~^ DBNO death_bledout' within timer+3s, zero 'Script Error', no CrashDump, and each player's coop_isActive returns to 1 (respawned).
- evidence: dbno.scr:304-316 (bleedout_timer), 474-503 (dbno_die, marker :475), 521-589 (teamrevive scan)  | risk: low - all players briefly downed then die+respawn on a scratch map

### [P1 / full] Voidguard out-of-bounds recovery, per-player isolation (4p)
- catches: void-fall warp, self-calibrated floor (coop_voidZ), restore to the OLDER good pos (never the slip edge), and that only the fallen player is warped while the other 3 are untouched. Failure classes: mis-calibration, warp-to-slip-edge, wrong-player warp, player lost in void.
- drive: Boot 4p, let voidguard's 8s calibration settle, then harness server-teleports ONE player's origin far below floor (e.g. z = -20000). Server-side origin teleport is permitted and reliable.
- verify: PASS = exactly one '^~^~^ VOIDGUARD recovered player at <vec>' for the fallen player; that player isAlive after; NO recovered-marker for the other 3 and their origins unchanged; fallen player's restored Z is a previously-recorded ground pos, not the -20000 drop.
- evidence: voidguard.scr:22-55 (marker :51; older-pos restore :44-48; spawnFallProtection call :50)  | risk: low

### [P1 / full] LMS lives decrement, spectator-force, and all-down game-over (4p)
- catches: per-player coop_deaths increment (player.scr:1324-1326), lmsForceSpectatorHandle forcing spectator at 0 remaining, missionfailed game-over when all out (main.scr:1449-1455), AND the latent parameter-aliasing bug main.scr:1444 where 'local.player = $player[local.i]' inside the counting loop clobbers the monitor thread's own player param - with 4 players a monitor can silently start tracking the wrong (last-indexed) player.
- drive: seta coop_lmsLives 1; boot 4p; harness kills players one at a time via server 'player damage $world 9999 ...' with a gap between each, then kills the last.
- verify: RECOMMEND adding '^~^~^ LMS spectate p=<name> deaths=<n>' in lmsForceSpectatorHandle and '^~^~^ LMS gameover' before missionfailed. PASS = 4 DISTINCT netnames each get a spectate marker (a repeated single name exposes the :1444 aliasing bug), then one gameover marker. Fallback grep (no new marker): literal 'LMS failed all players are dead or inactive.' + four 'You have 0 lives left'.
- evidence: player.scr:1322-1335 (decrement+call), main.scr:1414-1470 (force spectator :1425-1456, game-over :1449-1455, aliasing bug :1444)  | risk: medium - drives the real missionfailed/map-reload path; run last in a boot

### [P1 / full] DBNO corpse-revive window (killed-while-down -> stand up in place)
- catches: dbno_corpse_monitor recording corpseOrigin/Time and normalizing dead=1 (dbno.scr:248-254), corpse_revive window gate (:279), LMS no-lives gate (:281), and the in-place respawn warp (player.scr:947-955). Plus the negative case: revive attempted after the window must NOT stand the player up.
- drive: New probe dbnocorpse_run (coop_dbnocorpse 1): seta coop_dbnoCorpseRevive 10; dbno_enter host; wait for coop_dbno_active; 'player damage $world 9999' to kill while down; capture origin; within 10s thread dbno_corpse_revive <p>. Negative sub-run: wait >10s before the revive call.
- verify: PASS(positive) = '^~^~^ DBNO death_killedWhileDown' then '^~^~^ DBNO getup_corpserevive'; player alive; respawn origin within ~32u of the recorded corpse origin. PASS(negative) = death_killedWhileDown present but NO getup_corpserevive.
- evidence: dbno.scr:237-260 (corpse_monitor), 269-301 (corpse_revive, marker :292), player.scr:947-955 (in-place warp)  | risk: low

### [P1 / full] Spawn protection blocks damage in-window and restores after (4p)
- catches: spawnProtection nodamage window + coop_spawnProtOn re-arm guard + takedamage restoration (player.scr:1214-1237). Failure classes: window never clears (players permanently immortal), damage restored on a dead body, or fast respawn cycle cutting the window short.
- drive: New probe spawnprot_run (coop_spawnprot_test 1): seta coop_spawnProtect 4; on a fresh (re)spawn apply 'player damage $world 200'; read health (expect unchanged); wait past 4s; apply 'player damage $world 200' again; read health (expect dropped). Also fire two respawns quickly to exercise the re-arm guard.
- verify: Emit '^~^~^ SPAWNPROT p=<n> phase=in hpBefore=<a> hpAfter=<a>' (equal => blocked) and 'phase=out hpBefore=<b> hpAfter=<c>' with c<b (=> restored). PASS requires both.
- evidence: player.scr:1214-1237 (window, re-arm guard :1223-1224, restore :1231-1236)  | risk: low

### [P2 / partial] Respawn inventory + exact-ammo restore correctness (4p)
- catches: respawn kit rebuild + coop_ammoRestore (player.scr:963-974) - the exact-ammo-on-respawn feature and the regression class from bugs 618/912/913 (respawn hands DEFAULT ammo, drops the picked sidearm, or double-gives a primary).
- drive: New probe respawnkit_run: snapshot host primary weapon + reserve ammo; spend ammo to a known odd value; 'player damage $world 9999'; after respawn enumerate weapons + ammo.
- verify: Emit '^~^~^ RESPAWNKIT weap=<primary> ammo=<n> primaries=<count>'. PASS = ammo == the pre-death odd reserve (NOT the default 40) and primaries==1 (no duplicate primary).
- evidence: player.scr:920-974 (wasRespawn snapshot, ammoRestore :972-974), dbno.scr:646-698 (revive re-give chain, ammoRestore :698)  | risk: low

### [P2 / full] DBNO disabled on jeep/rail maps
- catches: dbno_start early-end when level.coop_dbno_disabled==1 (dbno.scr:39-45) - on m1l3a/m1l3b/t2l2 a lethal hit must produce a NORMAL death, never crawl-DBNO glued to a moving vehicle.
- drive: Boot a jeep map (m1l3a via ui_dmmap+ui_startdmmap 2); apply lethal 'player damage $world 9999' through the real monitor path (do NOT call dbno_enter directly - the gate lives in dbno_start/monitor, not dbno_enter).
- verify: PASS = NO '^~^~^ DBNO down p=' for that kill, player transitions straight to dead + respawn (manageDead runs), zero Script Error.
- evidence: dbno.scr:39-45 (coop_dbno_disabled guard), 43-45 (coop_dbno cvar gate)  | risk: low

### [P2 / full] Bleedout acceleration while crawling + chip-damage self-heal
- catches: dbno_crawl_manager movement penalty (dbno.scr:442-468): moving >100u from the down origin adds elapsed and prints once; and the 50% incoming-damage self-restore (:462-465) that stops a crawling player dying to chip damage before the timer.
- drive: Probe: down two players with equal coop_dbnoTimer; leave one stationary as control; server-teleport the other in >100u origin steps repeatedly for a fixed wall-time; sample coop_dbno_elapsed on both.
- verify: Emit '^~^~^ DBNOCRAWL p=<n> moved=<0/1> elapsed=<e>'. PASS = moved player's elapsed grows faster than the stationary control over identical wall-time, and its death_bledout arrives sooner.
- evidence: dbno.scr:442-468 (penalty accrual :444-453, self-heal :461-465)  | risk: low

### [P2 / full] DBNO death-race guards (outran + manageDead re-entry)
- catches: dbno_enter death_outran guard (dbno.scr:143-148) and manageDead cumulative re-entry into DBNO with the bug-924 healthonly-on-dead guard (player.scr:1298-1307) - a killing hit landing between the monitor check and dbno_enter must not throw 'cannot give health to dead entities' or abort a manageDead pass.
- drive: Probe: (a) kill the host first, THEN thread dbno_enter on the corpse to force the outran branch; (b) apply a single instant-kill exceeding coop_dbnoThreshold in one hit to exercise the cumulative re-entry in manageDead.
- verify: PASS = '^~^~^ DBNO death_outran' emitted for (a); NO 'Script Error' line containing 'cannot give health to dead' for either; player still reaches normal death+respawn.
- evidence: dbno.scr:143-148 (outran guard+marker), player.scr:1298-1307 (manageDead re-entry, bug-924 isAlive guard :1298)  | risk: low

### [P2 / needs-human] Team revive completion via real held [USE] (4p)
- catches: dbno_teamrevive_monitor proximity+useheld channel to completion -> dbno_ai_revive getup (dbno.scr:542, 567-575, 634). The existing coop_dbnoteam probe validates reviver-detection, teleport/proximity and that the monitor runs, but a minimized client cannot sustain +use, so completion is unverified by automation (HARD CONSTRAINT).
- drive: Existing coop_dbnoteam 1 probe (teleports reviver onto downed) PLUS a human holding [USE] (F) on the reviver client standing over the downed player for coop_teamReviveTime.
- verify: PASS = '^~^~^ DBNO getup_teamrevive p=<name>' and DBNOTEAM revived=1 alive=1. Auto pre-check (no human) still confirms DBNOTEAM begin/hold + monitor alive.
- evidence: dbno.scr:521-589 (useheld gate :542, completion :567-575), 596-713 (dbno_ai_revive, marker :634), coop_selftest.scr:164-217  | risk: low

### [P2 / needs-human] Self-revive completion via real held [USE]
- catches: medkit.scr:270-306 hold channel, medkit decrement (:302), move-cancel (:287-291), and the mid-channel-killed -> corpse_revive handoff (:431-434). Held [USE] cannot be automated headless.
- drive: Down host with a medkit present (coop_medkits 1); human holds [USE] ~5s without moving. Sub-case: start hold then move >10u to trigger cancel.
- verify: PASS = '^~^~^ DBNO getup_selfrevive p=<name>'; coop_medkits decremented by 1; player alive. Cancel sub-case shows no getup and 'Cannot move while applying health kit'.
- evidence: medkit.scr:214-306 (channel :270-306, marker :300, decrement :302), 431-434 (killed-mid-channel corpse handoff)  | risk: low

### [P3 / needs-human] Give-up (letgo) vs medic-callout on [USE] tap/hold
- catches: dbno_letgo_monitor: hold 60 frames with NO medkit = give-up death (dbno.scr:790-809), quick tap (<20 frames) = medic callout (:816-820), and the medkit-vs-give-up ownership split that keeps the two off the same key (:783). Needs real held/tapped [USE].
- drive: Down player with coop_medkits forced 0; human (a) taps [USE] once, then (b) holds [USE] ~3s.
- verify: PASS(tap) = broadcast 'is calling for a medic!' + a coop_dbno_call sound, NO death. PASS(hold) = '^~^~^ DBNO death_letgo' + player dies/respawns.
- evidence: dbno.scr:742-831 (useheld :774, give-up :790-809 marker :795, tap-callout :816-820)  | risk: low

### [P3 / needs-human] Headshot instant-kill DBNO bypass
- catches: dbno_monitor head-location instakill path (dbno.scr:111-116) gated on coop_dbnoHitLocation 1 - a genuine head-hit that crossed the DBNO threshold must skip DBNO and emit death_headshot rather than downing.
- drive: seta coop_dbnoHitLocation 1; a human (or a scripted AI with a headshot) lands a real head-location hit exceeding coop_dbnoThreshold on a player. Cannot be forced purely server-side because dbno_classifyHit reads self.fact.location from a real damage event.
- verify: PASS = '^~^~^ DBNO death_headshot' with NO preceding 'DBNO down p=' for that hit; player goes straight to death.
- evidence: dbno.scr:53-76 (classifyHit uses self.fact.location), 111-116 (headshot branch+marker)  | risk: low


## XP / challenges / service record / emblems + audio (coop_mod/xp.scr, challenges.scr, deathvox.scr, tinnitus.scr, dbno.scr sfx-duck)

### [P1 / full] XP kill-ownership: right player credited, no cross-credit (4p)
- catches: Array-index / ownership bugs where a kill credits the wrong $player index or the whole team. xp_ai_killed threads award to local.killer only; a regression that swapped index or used $player[1] would silently misattribute all XP.
- drive: New probe xptest_owner (gate coop_xptest 1) in coop_selftest.scr. With 4 connected players: for i in 1..$player.size, snapshot every player's flags["coop_xp_total"] and flags["coop_xp_b_kills"]; fabricate a victim (spawn an actor OR a stub listener with .team="german", .fact.location=99 (non-headshot), .coop_xp_hp0=100, no assist ledger); call waitthread coop_mod/xp.scr::xp_ai_killed victim $player[i]; wait 1 frame; re-read all four totals. Repeat per i. Requires coop_xp_enabled 1 (default) and seta coop_xp_scale 1.
- verify: Per i emit '^~^~^ XPOWN idx=<i> killer=<netname> gainSelf=<d> gainOthers=<sum> total=<n>'. PASS when gainSelf>0 AND gainOthers==0 for every i; FAIL (misattribution) if any gainOthers>0. Also grep 'Script Error' in the window = FAIL.
- evidence: coop_mod/xp.scr:480-602 (xp_ai_killed credits local.killer); aihandler.scr:907-915 (attacker->killer); assist read key entnum xp.scr:591-592  | risk: Low - direct function call on a throwaway victim; no map state mutated. Give players real XP (persisted to cvar/file) - run on a disposable test guid or reset coop_xp_<id> after.

### [P1 / full] xp_award NIL/NULL/dead-ref regression guard (bug-1065) on every map
- catches: Re-introduction of the 'cast none to listener' crash class: xp_award called with a freed player (NULL), an unset var (NIL, e.g. level.coop_smoke_player before any smoke), or the reason path that derefs .classname on none. bug-1065 was exactly a DBNO team-revive awarding to an uninitialized level.coop_smoke_player.
- drive: New probe xptest_nullguard (gate coop_xptest 1), run once per map after spawn: call waitthread coop_mod/xp.scr::xp_award NULL 10 "kills"; then xp_award NIL 10 "revive"; then xp_award level.coop_smoke_player 25 "support" (exercises the exact bug-1065 var); then take a live player, store ref, then feed a deliberately-freed/expired ref if obtainable. Also directly call the two real callers with a NULL owner: dbno.scr revive path with level.coop_smoke_player unset.
- verify: Emit '^~^~^ XPNULL map=<map> passed=1' only after ALL four calls return. Harness FAILS the map if 'Script Error' (esp. 'cast' / 'none') appears between the probe-begin marker and XPNULL passed=1, or if XPNULL passed=1 never prints (thread died on the bad deref).
- evidence: xp.scr:326-333 (NULL||NIL + classname guards); variables.scr:76 (coop_smoke_player init NULL, the bug-1065 fix); dbno.scr:600 (xp_award level.coop_smoke_player)  | risk: Very low - guards are designed for exactly these inputs; if they regress the thread dies harmlessly (no server crash) and the missing marker flags it.

### [P1 / full] Assist attribution across 4 players (25% dmg / 10s window)
- catches: Wrong-index or key-mismatch in the assist ledger: killer gets the kill, every OTHER player who dealt >=25% of victim start-HP within 10s gets 'Assist', and players who dealt nothing get nothing. Write key is attacker.entnum (aihandler) and read key is p.entnum (xp) - a divergence would zero all assists or credit the wrong player.
- drive: New probe xptest_assist: fabricate victim with coop_xp_hp0=100; seed victim.coop_xp_dmg[$player[1].entnum]=30, coop_xp_dmgT[..]=level.time (fresh); seed $player[2] with 30 dmg but dmgT=level.time-15 (stale, should NOT pay); seed $player[3]=10 dmg fresh (below 25%, no pay); leave $player[4] with nothing; call xp_ai_killed victim $player[1] (killer). Snapshot each player's coop_xp_b_kills before/after.
- verify: Emit '^~^~^ XPASSIST killer=1 assist2=<0|1> assist3=<0|1> assist4=<0|1>'. PASS = assist2==0 (stale >10s), assist3==0 (<25%), assist4==0 (no dmg), and killer credited. FAIL if a stale/under-threshold/zero player was paid an assist.
- evidence: xp.scr:582-601 (assist loop, need=hp0*0.25, 10s window); aihandler.scr:550-556 (ledger write per entnum)  | risk: Low - seeds transient fields on a throwaway victim; awards a few XP to real player files (reset after).

### [P1 / full] Challenge progress persists across a map transition, no double mission-award
- catches: Write-through persistence loss (progress not surviving stuffsrv 'map') AND double-award of the per-map/mission team feat. chal_mission_complete guards with level.coop_chal_missionPaid (a LEVEL var that must reset per map); a regression to a global/flags var would double-pay 'missions' and map_<name>, or a lost cvar would reset per-weapon progress on every map.
- drive: Map A: probe bumps a counter challenge (e.g. chal_bump $player[1] "wpn_thompson" 3) and reads back coop_chalP_<cid>; force chal_save_player. Fire wintest_run to transition A->B via the REAL missioncomplete path. On map B (after chal_ensure loads from coop_chal_<id> cvar): re-read coop_chalP_<cid> and the 'missions' progress. Then on B, deliberately call chal_mission_complete twice in one frame and read 'missions' delta.
- verify: Emit on A '^~^~^ CHALPERSIST map=A wpn=<3> saved=1'; on B '^~^~^ CHALPERSIST map=B wpn=<n> missionsBefore=<x> missionsAfterDouble=<y>'. PASS: n==3 (survived), and y==x+1 (double call paid once, missionPaid held). FAIL if n<3 (lost) or y>x+1 (double-award).
- evidence: challenges.scr:1292-1298 (coop_chal_missionPaid guard + team_bump); challenges.scr:554-562 (write-through cvar+file); challenges.scr:432-439 (ensure loads cvar-first); missioncomplete.scr:47 (chal_mission_complete hook)  | risk: Medium - drives a real map transition (bsptransition) mid-test; use two known-good coop maps and the wintest delay so prespawn settles. Writes to the test guid's save files.

### [P2 / full] Savior + Guardian valor bonuses (4p proximity/targeting)
- catches: The two valor paths that depend on OTHER players: Savior (victim.fact.enemyAtPain is a DIFFERENT player than the killer) and Guardian (a downed teammate within 512u of the victim). Both iterate $player and compare entities - prime spots for a self-vs-other identity bug that pays the killer for saving himself, or misses a genuine save.
- drive: New probe xptest_valor with 4 players: (a) victim.fact.enemyAtPain=$player[2], call xp_ai_killed victim $player[1] -> expect Savior on p1, saviorPaid=1. (b) enemyAtPain=$player[1] (self) -> expect NO Savior. (c) Guardian: no enemyAtPain; teleport $player[3] within 512u of victim.origin and set $player[3].flags["coop_dbno_active"]=1; call xp_ai_killed victim $player[1] -> expect Guardian valor on p1. Read p1 coop_xp_b_valor delta per case.
- verify: Emit '^~^~^ XPVALOR case=<a|b|c> valorGain=<d>'. PASS: a>0, b==0, c>0. FAIL if self-target paid Savior (b>0) or Guardian missed (c==0).
- evidence: xp.scr:514-521 (Savior, enemyAtPain != killer); xp.scr:559-573 (Guardian scan of downed teammates <512u)  | risk: Low - sets coop_dbno_active flag on a real player transiently; clear it after so player.scr manage loop is not left confused (set back to prior value).

### [P2 / full] Per-weapon challenge credits the HELD weapon, not the previous gun
- catches: The bug the chal_weaponId re-query fixed (bug-721 era): killing with an imported weapon (.30cal, mg42 portable, xw guns) credited the PREVIOUSLY carried gun because the cached coop_activeWeapon flag lagged. Also verifies the team-gate: teamkilling an allied (non-german) victim must NOT bump any weapon challenge.
- drive: New probe xptest_wpn: give $player[1] a specific weapon (e.g. item models/weapons/thompson.tik) so returnActiveWeapon resolves it; fabricate a german victim; snapshot coop_chalP_<wpn_thompson_cid>; call chal_ai_killed victim $player[1]; re-read. Then swap the player to a .30cal (imported) and repeat, asserting wpn_thirtycal bumps not the stale thompson. Third case: victim.team="american" -> assert NO kill-family bump.
- verify: Emit '^~^~^ CHALWPN held=thompson bumped=<thompson|other|none>', '^~^~^ CHALWPN held=thirtycal bumped=<...>', '^~^~^ CHALWPN teamkill kill=<0|1>'. PASS: each held gun bumps its own id, teamkill kill==0.
- evidence: challenges.scr:807-866 (chal_weaponId re-queries returnActiveWeapon, flag=fallback); challenges.scr:931-939 (team!=german early-out + wpn_ bump)  | risk: Low - takeall/give on the host player transiently (restore m1_garand after, as weaptest_run does).

### [P2 / full] XP kill soft-cap scales with ACTIVE player count (4p anti-farm)
- catches: xp_killcap_factor counts only players with coop_isActive==1 and scales the 90/180-kills-per-player thresholds by that count. A miscount (counting dormant host, or all $player regardless of active) would crush 4-player XP prematurely or never cap. This is a pure 4-player-dimension test.
- drive: New probe xptest_cap: with 4 active players, seed level.coop_xp_teamkills to values around the boundaries and call xp_killcap_factor: at teamkills=300 (<90*4=360) expect factor 1.0; at 400 (>360, <720) expect 0.5; at 800 (>720) expect 0.25. Then flip one player's coop_isActive to 0 (np=3) and re-check that 300 now yields 0.5 (>90*3=270). Restore flags.
- verify: Emit '^~^~^ XPCAP np=<n> teamkills=<t> factor=<f>' per case. PASS = factor matches the np-scaled thresholds (1.0/0.5/0.25). FAIL if factor ignores np or uses wrong player count.
- evidence: xp.scr:436-453 (xp_killcap_factor: counts coop_isActive, thresholds 90*np / 180*np)  | risk: Low - only reads $player and a level var; restore coop_isActive and coop_xp_teamkills after.

### [P2 / full] Rank-up fires once + unlock grant dedups (per-player, 4p independent)
- catches: Multi-rank lump-jump handling and emblem/weapon unlock dedup. xp_award promotes across every crossed rank via xp_rank_unlock(prev+1..new); chal_add_unlock must dedup so a reconnect/backfill does not flood the NEW UNLOCKS queue. A regression could double-fire the promo ceremony or re-queue already-owned unlocks.
- drive: New probe xptest_rank: for $player[1], set flags["coop_xp_total"] just below rankXP[3]=2200 and coop_xp_rank=2; call xp_award $player[1] 5000 "kills" (jumps several ranks at once). Read coop_xp_rank after and count how many entries chal_add_unlock added to coop_chal_unlocks (snapshot before/after). Then call xp_rank_unlock $player[1] 0 <rank> again (simulating reconnect backfill) and assert the unlock set size is UNCHANGED. Do the same on $player[2] with different totals to confirm per-player independence.
- verify: Emit '^~^~^ XPRANK p=1 fromRank=2 toRank=<r> unlocksAdded=<a> unlocksAfterRebackfill=<b>'. PASS: r reflects the 7200 total, a>0, b==a (dedup held). FAIL if b>a (re-queue flood) or rank not advanced.
- evidence: xp.scr:368-376 (promo + xp_rank_unlock prev+1..new); xp.scr:276-278 (connect-time backfill 0..rank, idempotent); challenges.scr:699-719 (chal_add_unlock delimiter dedup)  | risk: Low-medium - awards large XP + triggers the on-screen promo ceremony (harmless in headless); writes rank/unlocks to test guid files (reset after).

### [P2 / full] No-down campaign feat: active-only + downed-player exclusion (4p)
- catches: cc_award_clean and the mission-complete playstyle feats must award ONLY active players who never went down this map. With 4 players where one was downed, the downed player must be excluded while the other three are credited - a per-player scoping bug (coop_wentDownMap stamp vs mapname) would over- or under-award.
- drive: On a live map with 4 players: down $player[2] via dbno_enter (stamps coop_wentDownMap=mapname), leave 1/3/4 clean; ensure all coop_isActive==1. Call cc_award_clean "cc_earn_this" (or a synthetic clean stat) and read each player's coop_chalP_<cid>. Also flip $player[4] coop_isActive=0 and confirm it is skipped.
- verify: Emit '^~^~^ CCCLEAN p1=<0|1> p2=<0|1> p3=<0|1> p4=<0|1>' (1=feat bumped). PASS: p1==1,p3==1 (clean+active), p2==0 (downed), p4==0 (inactive). FAIL on any mismatch.
- evidence: challenges.scr:790-800 (cc_award_clean: coop_isActive && coop_wentDownMap != mapname); dbno.scr:163 (coop_wentDownMap stamp on down); challenges.scr:1309-1332 (mission-complete per-player feats)  | risk: Medium - actually downs a real player (dbno_enter); revive/restore afterward so the manage loop is clean. Run on a stable map.

### [P2 / partial] Late-join XP/challenge identity: single load, no double-init (4p)
- catches: The connect-time identify race: a kill can call xp_identify concurrently with the connect hook; the singleton guard (coop_xp_identifying) and chal_ensure loading guard must serialize so a late (4th) joiner mid-map loads its saved total exactly once and awards never see a half-initialized total (the NIL-HUD bug).
- drive: Boot 3 players; on a live map, seed a known saved total for a 4th guid (setcvar coop_xp_<guid> 5000 + write file). Connect the 4th client (harness late-join). Immediately (same second) fire two concurrent xp_award calls on it plus rely on the connect-time xp_identify. Probe polls the 4th player's coop_xp_total and coop_xp_rank once identify settles.
- verify: Emit '^~^~^ XPJOIN id=<guid> total=<n> rank=<r> identCount=<c>' where a probe counter increments each time xp_identify passes the guard body. PASS: total==5000+awards, c==1 (loaded once). FAIL if c>1 (double load) or total==0 (award raced an empty init).
- evidence: xp.scr:224-284 (xp_identify singleton coop_xp_identifying); xp.scr:330-333 (award triggers identify if unset); challenges.scr:402-413 (chal_ensure loading guard)  | risk: Low - needs the harness to time a 4th connect; if connect timing is loose the race window may not be hit (still validates single-load, just not the tightest race).

### [P2 / partial] Death-voice proximity band + native-VO mute chooses by CLOSEST of 4 players
- catches: deathvox scans ALL $player for the minimum distance and picks near/mid/far band (and whether to mute native German VO). With 4 spread-out players a regression that uses $player[1] instead of the closest, or casts $player with 2+ connected, would pick the wrong band or throw. The AUDIBLE output is client-side (playsound) so only the band DECISION is log-verifiable.
- drive: New probe audtest_band: place 4 players at known ranges from a fabricated dying ent (e.g. p1 far, p2 within coop_deathvox_range=1500, p3/p4 far). Instrument a probe wrapper that calls the same min-distance scan as deathvox.play and emits the chosen band + muteNative decision (add markers inside a coop_audtest-gated branch of deathvox.play, OR replicate the scan in the probe reading the same cvars). Vary which player is closest across sub-cases.
- verify: Emit '^~^~^ AUDBAND mindist=<d> band=<near|mid|far> mute=<0|1> closest=<idx>'. PASS: band matches the actual minimum distance and closest==the intentionally-nearest index; no 'cast array to listener' error with 4 connected. Actual grunt audio = needs-human spot-check.
- evidence: deathvox.scr:30-58 (min-distance scan over $player, band select, muteNative); deathvox.scr:66-76 (native-VO mute channels 4/7/8)  | risk: Low - read-only distance math on a throwaway ent; the audible pass still needs a human ear.

### [P3 / needs-human] Tinnitus / sfx-duck s_volume issuance is per-client (log the decision, hear the effect)
- catches: The per-player s_volume duck (tinnitus muffle, DBNO bleed-out, rank-up xp_ping_duck) is stuffed to ONE client via stufftext - it must target only the rocked player, not broadcast. With 4 players a broadcast bug would duck everyone's audio on one player's blast. s_volume is a client cvar so only the SERVER-SIDE decision (which player, when) is log-verifiable.
- drive: New probe audtest_duck: on a live map, call the trigger path for $player[2] only (e.g. simulate a big hit by lowering health past coop_tinnitusMinDamage, or directly thread coop_tinnitus_trigger on p2; and separately call xp_ping_duck $player[2]). Instrument the s_volume stufftext sites (tinnitus.scr / xp.scr:308-320 / dbno.scr:353) to also println a coop_audtest-gated marker naming the target player.
- verify: Emit '^~^~^ AUDDUCK src=tinnitus target=<netname> vol=0.20' etc. PASS: exactly one marker naming p2 per trigger, no marker for p1/p3/p4. FAIL if multiple players targeted. The actual muffle/ring = needs-human (client s_volume/playlocalsound not in qconsole.log).
- evidence: tinnitus.scr:14-45 + playlocalsound (per-player ring); xp.scr:308-320 (xp_ping_duck per-client stufftext s_volume); dbno.scr:353,364,700 (bleed-out + restore s_volume)  | risk: Low - transient health nudge / direct trigger on one player; audible verification requires a human client.

### [P3 / full] Micro-popup 3-slot contention: 4 simultaneous awards, no leak/overwrite
- catches: xp_micro_popup has only 3 HUD slots (73/74/75); a 4th simultaneous award must be DROPPED, not overwrite a live slot or leak the coop_xp_mpNN busy flag (which would permanently suppress future popups for that player). 4-player play routinely lands 4+ near-simultaneous awards on one player (multi-bonus kills).
- drive: New probe xptest_popup: on one player, fire 4 xp_micro_popup calls in the same frame (e.g. via 4 back-to-back xp_award with distinct labels). After the popup fade window (~2s), read player.flags["coop_xp_mp73/74/75"] and confirm all cleared back to NIL.
- verify: Emit '^~^~^ XPPOPUP fired=4 slotsUsed=<n> leaked=<0|1>' where leaked=1 if any coop_xp_mpNN stayed 1 after the fade. PASS: slotsUsed<=3, leaked==0, no Script Error. FAIL on a stuck busy flag (future popups dead).
- evidence: xp.scr:402-430 (3-slot cap, 4th dropped, flag cleared on fade end)  | risk: Very low - HUD-only, self-clearing; worst case a cosmetic stuck flag on the test player.


## Weapon systems + combat FX (roster/holster, exact-ammo, ammo box, MG42 overheat, decap/gore skins, blood trails, suppression, smoke-vs-frag dedup, headshot kill sound)

### [P1 / partial] FIRETEST: AI-driven fire sweep of the 69-gun roster (fire-anim/muzzle/projectile tik integrity, beyond give)
- catches: The existing weaptest only GIVES each roster weapon (coop_selftest.scr:69-104) which loads the world-model tik but never exercises the fire animation, muzzle FX tik, or projectile tik - those are separate precache/anim paths that fail silently until a shot is fired. Server-side +attack can't be forced on a minimized client, so drive it via an AI actor that fires naturally when it has an enemy.
- drive: New probe firetest_run in coop_selftest.scr gated on coop_firetest 1. waitthread loadoutroster.scr::roster_ids to get level.coop_loRosterN/Give[]. Spawn one test actor + a hated target dummy near a player; loop the 69 tiks: actor takeall; actor item <tik>; make it engage (set enemy/forceanim fire) ~1.5s; then next. developer 1 REQUIRED so compile/anim errors print.
- verify: Emit '^~^~^ FIRETEST idx=N id=X tik=Y fired=1' before advancing each gun. FAIL = any 'Script Error' or TIKI/anim error line between a gun's fired marker and the next (attributable to that exact gun), or a missing fired marker (actor never took the weapon). PASS = 69 fired markers, zero Script Error in the window.
- evidence: coop_mod/coop_selftest.scr:84-97; coop_mod/loadoutroster.scr:566-577  | risk: Low - AI actor + dummy on an already-loaded map; some special weapons (mine detector, smokes, riflegrenade) AI won't voluntarily fire, so those fall through to a needs-human spot-check.

### [P1 / full] EXACTAMMO: 4-player concurrent-respawn reserve restore (per-self flag isolation)
- catches: coop_ammoSnapshotLoop stores reserves in per-self flags coop_ammoSaved_<type> and coop_ammoRestore writes them back on respawn (itemhandler.scr:2504-2559). With 4 players dying/respawning in the same window, a bug in the per-self keying or the coop_ammoRestorePending gate (bug-913) would cross-contaminate or clobber one player's saved counts with another's / with defaults.
- drive: seta coop_exactAmmo 1 in boot cfg. Probe: for each of the 4 alive players set a UNIQUE known reserve (p1 rifle 7, p2 rifle 13, p3 rifle 23, p4 rifle 31) via 'setammo', wait >0.5s for a snapshot tick, then kill all four in one frame (self.health = 0 for each in a tight loop), let the respawn+coop_ammoRestore run, then read getammo rifle for each.
- verify: Emit '^~^~^ EXACTAMMO p=<idx> saved=<N> got=<M> ok=<0/1>' per player (ok = got==saved). FAIL = any ok=0, or two players sharing the same got value (cross-bleed).
- evidence: coop_mod/itemhandler.scr:2487-2519 (snapshot), 2529-2559 (restore), 2507 pending-gate  | risk: Low - server-side setammo/getammo/health, no client input; killing all 4 is the normal respawn path.

### [P1 / full] GOREBUDGET: decap/gib + gore-skin entity budget under 4-player concurrent kills (2048-pool crash class)
- catches: Every kill spawns a decap gib Animate (sentient.cpp:3497-3505, PostEvent EV_Remove 1) and drives CoopGoreUpdateSkinTier + drip/pool decal chains. Four players wiping a squad in the same frame is exactly the concurrent-alloc pattern that stomped the entity pool in the bugs 914-927 saga. This test forces the worst-case concurrent kill burst and watches for pool exhaustion / crash / Script Error.
- drive: seta coop_goreDebug 1, coop_bloodDebug 1. Probe spawns a cluster of ~12-16 test actors around the 4 players, then applies lethal (or gib-level) damage to all of them in one frame loop. Harness samples server memory + proc-alive before/after and greps CrashDumps. Repeat the burst 5x.
- verify: Watch engine '^~^~^ GORESKIN ent=N tier=3 ...' (sentient.cpp:2292) markers - count must track kills without a runaway flood; harness asserts proc still alive, no new CrashDump, memory returns near baseline after gibs EV_Remove. FAIL = crash/exit, 'AllocEdict'/pool error, or unbounded entity/memory growth across the 5 bursts.
- evidence: fgame/sentient.cpp:3497-3506 (decap gib spawn+sound), 2231-2292 (gore skin tiers); memory: entity_pool_2048  | risk: Medium - deliberately stresses the entity pool; that IS the point, but run it isolated so a crash is cleanly attributable.

### [P2 / full] SMOKEFRAG: smoke grenade must not dedup frags out of inventory (4P, per-player)
- catches: itemhandler dedups weapons to one-per-ammo-class; smokes were given their OWN 'smokegrenade' class (itemhandler.scr:2288-2293) and coop_smoke_grenade.tik is exempted from the frag-strip re-add (2468) specifically so carrying a smoke never collapses the player's frag pool. A regression in the case-mapping would silently strip agrenade/grenade when a smoke is held - and it must hold for all 4 players independently.
- drive: Probe gives each of the 4 players BOTH a frag (item mills_grenade.tik -> agrenade) and a smoke (item m18_smoke_grenade.tik -> smokegrenade), then triggers the inventory rebuild/juggle path, then reads getammo agrenade and getammo smokegrenade per player.
- verify: Emit '^~^~^ SMOKEFRAG p=<idx> frag=<N> smoke=<M> ok=<0/1>' (ok = frag>0 && smoke>0). FAIL = any player with frag=0 while smoke>0 (smoke stripped the frag).
- evidence: coop_mod/itemhandler.scr:2284-2293 (class map), 2468 (smoke exempt from strip)  | risk: Low - pure give+read, no combat.

### [P2 / full] BLOODTRAIL: blood-trail decal/entity budget under sustained 4-player combat + limping wounded
- catches: coop_bloodTrail drops a floor decal every coop_bloodTrailInterval (0.45s) per bleeding sentient over coop_bloodTrailDist (sentient.cpp:2632-2674). Combined with the 'wounded Germans limp to a medkit' behavior (task #26), a long fight with many bleeding movers is a sustained decal/entity generator - test for unbounded growth or pool pressure over a multi-minute soak, not a one-shot.
- drive: seta coop_bloodTrail 1, coop_bloodDebug 1. Probe spawns a rolling population of wounded (sub-lethal) AI that move around the 4 players; sustain for ~3 min. Harness samples memory + entity-count on an interval and counts BLOODTRAIL markers.
- verify: Engine '^~^~^ BLOODTRAIL PLAYER/AI ent=N splat=... imgidx=M frac=...' (sentient.cpp:2700-2707) gives per-drop telemetry. PASS = marker rate steady, memory slope flat across the soak, proc alive, imgidx never 0 (0 = unregistered splat shader -> white-decal bug). FAIL = rising memory/entity slope, pool error, or imgidx=0.
- evidence: fgame/sentient.cpp:2632-2707  | risk: Low-medium - long soak; watch it doesn't mask an unrelated leak.

### [P2 / partial] AMMOBOX: per-player resupply cap + 4-player contention over one box
- catches: A deployed ammo box grants EACH squadmate a private number of refills tracked in level.coop_ammobox_uses[slot][playerkey] (ammobox.scr:205), and refuses once a given player hits maxn while other players are unaffected. Bugs here (shared vs per-player counter, wrong key) would let one player drain everyone's uses or bypass the cap.
- drive: Probe: player A calls ammobox_place (verify the one-box-per-mission gate at line ~22 fires on a 2nd place). Then, per player, drive the resupply and inspect level.coop_ammobox_uses[slot][key] after each. The honest path (proximity + held [USE], ammobox.scr:202-233) needs a real held button; the counter/refill logic (ammobox_refill, line 70) is inspected via probe.
- verify: Emit '^~^~^ AMMOBOX p=<idx> n=<uses> refilled=<0/1>'. PASS = each player independently reaches maxn, a further attempt for a capped player gives refilled=0 while a fresh player still refilled=1. FAIL = one player's uses decrement another's, or cap bypassed.
- evidence: coop_mod/ammobox.scr:70-93 (refill), 155-233 (proximity+USE+per-player counter)  | risk: Low - the real proximity/[USE] hint path (held +use) is a needs-human spot-check; counter/refill is scriptable.

### [P2 / needs-human] MG42OVERHEAT: mounted-turret heat build -> lockout -> cooldown (STAT_MGHEAT mirror)
- catches: TurretGun heat builds 50/s while firing, overheats + locks out at 100 (steam hiss coop_mg_overheat), ignores fire input while hot, and cools 25/s back to 0; heat is mirrored to the gunner's STAT_MGHEAT HUD bar (weapturret.cpp:841-884). Regressions: never locks out, never recovers, HUD bar desync, or fire not ignored while overheated. Heat is driven by real sustained fire input (m_iFiring), which a minimized client can't transmit.
- drive: Human mounts an MG42 turret and holds +attack ~2.5s to hit 100, keeps holding to confirm shots stop, releases ~4s to confirm ready. For automation, first add a debug marker (proposed coop_mgHeatDebug) printing '^~^~^ MGHEAT heat=<0..100> over=<0/1>' in P_ThinkActive so a human-driven run is machine-verified; an AI-gunner variant (mg42_hack seats an AI) could drive it scriptably but the STAT mirror is player-only.
- verify: With the proposed marker: heat rises to 100 -> over=1 -> fire ceases -> heat decays to 0 -> over=0 ready. Also assert the coop_mg_overheat sound event fires once per cycle. FAIL = over never reaches 1, stays 1 forever, or shots continue while over=1.
- evidence: fgame/weapturret.cpp:841-884  | risk: Low - benign turret interaction; only blocker is the sustained held-fire input.

### [P3 / full] MG42HP: captured-MG42-gunner health scales by connected player count (4P)
- catches: mg42_hack.scr scales a converted MG42 gunner's health by $player.size, capped at 150 (mg42_hack.scr:9-13: health = 25 * $player.size, clamp 150, only applied when size>1). A $player array-cast or clamp bug would mis-scale the gunner (too weak solo-value at 4P, or overflow past the cap).
- drive: With 4 players connected, probe converts/seats an MG42 gunner (set self.flags['coopMG42_gunner']) and waitthread mg42_hack.scr::main, then reads self.health.
- verify: Emit '^~^~^ MG42HP players=<size> hp=<self.health>'. PASS at 4P = hp==100 (25*4, under the 150 cap); at 2P = hp==50; solo = base 25. FAIL = clamp exceeded (>150) or wrong multiple. Also asserts no 'cast array to listener' error from the $player.size read with 4 connected.
- evidence: coop_mod/mg42_hack.scr:9-14  | risk: Low.

### [P3 / partial] GORESKIN-CORPSE: heavy gib-splatter skin (tier 3) only on the dead, never on a moving enemy
- catches: Task #37 intent: living/moving AI may take light tier1/tier2 wound skins, but the extreme gib-splatter tier 3 must land only on a corpse (sentient.cpp:1728-1742 - killing blow leaves heavy corpse; CoopGoreUpdateSkinTier clamps at 3 and the tier-3 upgrade path is corpse-gated). A regression would paint tier 3 on an actor that is still alive/moving.
- drive: seta coop_goreDebug 1. Probe repeatedly applies SUB-lethal damage to a single moving AI (keep isAlive true) for several seconds, recording GORESKIN markers, then delivers the kill and records the post-death marker.
- verify: Engine '^~^~^ GORESKIN ent=N tier=T dmg=... model=...' (sentient.cpp:2292). PASS = while isAlive(ent) every marker has tier<=2; tier=3 appears only AFTER the kill. FAIL = any tier=3 emitted while the actor is still alive. (Recommend adding a deadflag field to the print to make the correlation exact rather than timing-inferred.)
- evidence: fgame/sentient.cpp:1728-1742, 2231-2292  | risk: Low - correlation currently relies on kill timing vs marker; small engine print tweak makes it fully deterministic.

### [P3 / needs-human] HEADSHOT: headshot kill-sound credited to the correct killer among 4 players
- catches: The headshot cue is a CHAN_LOCAL sound played on the KILLER (weaputils.cpp:2448-2454, owner->Sound gated on owner is player + victim is non-player sentient + head/helmet/neck hitloc + kill). In 4P, a bug attributing the kill/owner to the wrong player would play the cue for a bystander. It's a client-local sound with no server log signal today.
- drive: Add a debug print (proposed coop_headshotDebug) at the branch: '^~^~^ HEADSHOT killer=<owner entnum> victim=<ent entnum> loc=<trace.location>'. Position 4 players, have one land a head-region kill on an AI (real aimed fire).
- verify: PASS = killer entnum == the shooter who fired; victim is the AI; loc in {HEAD,HELMET,NECK}. FAIL = killer entnum is a non-shooting player, or cue fires on a body/torso kill.
- evidence: fgame/weaputils.cpp:2448-2454  | risk: Low - needs a real aimed head-kill (client fire) plus a small engine debug marker; no state risk.

### [P3 / partial] HOLSTER: weapons-on-back switch path runs clean for all 4 players (crash/error smoke)
- catches: The unholster/return-active-weapon path (itemhandler::returnActiveWeapon, invoked from mg42_hack.scr:18-23 and on weapon switch) plus the engine holsterOffset attach is where the weapons-on-back system historically broke (task #20). A per-player switch storm can surface Script Errors or a weaponless/unarmed state.
- drive: Probe gives each of the 4 players a primary + a secondary, then forces a rapid weapon switch on all four (self use <primary>; activatenewweapon; self use <secondary>) a few cycles. developer 1 REQUIRED.
- verify: Emit '^~^~^ HOLSTER p=<idx> switched=1' per player per cycle. PASS = no 'Script Error' and no returnActiveWeapon failure in the window for any player. FAIL = Script Error, or a player ends weaponless. Visual confirmation of the back-model attach is a needs-human spot-check (no log signal for the attached model).
- evidence: coop_mod/mg42_hack.scr:16-23; coop_mod/itemhandler.scr (returnActiveWeapon)  | risk: Low - give+switch only; visible-model correctness still needs an eyeball.
