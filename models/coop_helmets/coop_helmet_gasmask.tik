TIKI
// HZM coop [user 08-07] bug-1534 (Part 2 PROTOTYPE): EXACT-FIT headgear, same recipe as the other
// 34 pieces (geometry lifted from a worn gear skd, head-bone-local coords, cm scale -> 0.52,
// shared identity idle skc) - source: german_Wehrmacht_GasTrooper.tik's models/gear/gasmask.skd
// (248 verts, single-weighted 1.0 to "Bip01 Head" in the source skeleton - the golden case,
// same shape of transplant as us_helmet.skd -> us_helmetfit.skd). Built + verified with a
// from-scratch Python SKD writer (scratchpad/build_gasmask.py this session) since the original
// 07-12 gen_all_headgear.py/bake_skd2.py scripts no longer exist on disk; round-tripped through
// _research/assetaudit/skx.py (0 warnings) before deploy. NOT yet verified in-game - see report.
setup
{
	scale 0.52
	path models/coop_helmets
	skelmodel gasmask.skd
	surface gasmask shader gasmask_green
}

animations
{
	idle		us_helmetfit.skc
}
