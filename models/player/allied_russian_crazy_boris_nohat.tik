TIKI
// HZM coop [user 08-07] bug-1545 GENERATED HATLESS VARIANT of models/player/allied_russian_crazy_boris.tik -
// identical to the original with the head-covering skelmodel/surface lines removed,
// so the helmet switcher's chosen piece is the only thing on the head. Regenerate with
// _research/nohat/nohat_build.py; do not hand-edit. Standard Issue re-attaches the original
// headgear as a coop_std_*.tik piece (coop_mod/helmet.scr::level.coop_skinStdHelmet).
setup
{
	scale 0.52

	path models/human/German_worker
	skelmodel german_worker.skd

	surface hand shader handsnew
	surface shirt shader boris_shirt
	surface pants shader boris_pants

	path models/human/heads			// Set new path for head model
	skelmodel head2.skd			// Set head model
	surface head shader boris_head

	// HANDS
	// =======================================================
	path models/human/hands
	skelmodel hand.skd
	surface hand shader handssnow

	// GEAR
	// =======================================================
}

init
{
	client
	{
		// Cache sounds here
	}
	server
	{
		voicetype russian // set the voicetype to use for multiplayer instant messaging
	}
}

//
// include the base animations definition
//
$include models/player/base/include.txt
