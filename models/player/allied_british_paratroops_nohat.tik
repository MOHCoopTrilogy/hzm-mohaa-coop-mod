TIKI
// HZM coop [user 08-07] bug-1545 GENERATED HATLESS VARIANT of models/player/allied_british_paratroops.tik -
// identical to the original with the head-covering skelmodel/surface lines removed,
// so the helmet switcher's chosen piece is the only thing on the head. Regenerate with
// _research/nohat/nohat_build.py; do not hand-edit. Standard Issue re-attaches the original
// headgear as a coop_std_*.tik piece (coop_mod/helmet.scr::level.coop_skinStdHelmet).
setup
{
	path models/6abs	// Set path to set skelmodel from
	skelmodel brit_para.skd		// Set body model

	surface britparatunic shader tunic6abs
	surface britparapants shader pants6abs
	surface britparasleeve shader sleeve6abs
	   

//	$include models/human/heads/us_young_heads.tik
	path models/human/heads
	skelmodel head1.skd
	surface head shader britoffface6abs

	path models/human/hands			
	skelmodel hand.skd		
	surface hand shader handsnew

	path models/6abs

	path models/6abs
	skelmodel british_tank_holster.skd
	surface brittankholster shader brittankholster6abs

	path models/6abs
	skelmodel british_dagger.skd
	surface britdagger shader britdagger6abs

	path models/6abs
	skelmodel british_canteen.skd
	surface britcanteen shader britcanteen6abs

	path models/6abs
	skelmodel british_showel.skd
	surface britshowel shader britshowel6abs

	path models/6abs
	skelmodel british_ammo.skd
	surface britammo shader britammo6abs

	
}


//
// define what folder to get specific sound from for the player model
//
$define pulloutdir sound/weapons/pullout


init
{
	client
	{
		// Cache sounds here
	}
	server
	{
		voicetype airborne // set the voicetype to use for multiplayer instant messaging
	}
}


//
// include the base player model definition
//
$include models/player/base/include.txt

// need to indicate the end of the tiki file because we have a tiki command at the end
//end
