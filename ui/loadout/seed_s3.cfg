// [user 2026-08-18] SEED CHIP DISPLAY ONLY (deep-trace F9): plain `set`, session-local.
// Never writes coop_loA/FA (the archived replay recipes) and never re-commits - the seed
// was already committed server-side by loadout_seedDefaults.
set coop_lo3 "48"
set coop_loN3 "COLT 45"
set coop_loS3 "models/weapons/colt45.tik"
set coop_loInspectS3 "exec ui/loadout/p48.cfg"
