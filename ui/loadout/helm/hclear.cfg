// GENERATED - armory helmet tile CLEAR (No Helmet). The server pushes an exec of this file
// because a raw "set coop_loHelm" push with an empty value arrives as a 2-arg set, which
// PRINTS the cvar instead of setting it (bug-773). From a cfg the quoted empty string
// tokenizes as a real third argument, so this actually blanks the tile.
set coop_loHelm ""
