// HZM coop - A/B: turn the 18 angular joint limits OFF  [bound to 7]
// Restores the old constraint set exactly: the fold braces come back in the same breath the
// limits go away, so the pair can never half-ship. This is the direct before/after for
// "do the limits stop the mangling?"
coop_ragdollLimits 0
echo "RAGDOLL: joint limits OFF (fold braces restored). F8 turns them back on."
